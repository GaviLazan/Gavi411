// Clerk auth middleware (G411-13, [Agentic] — build this together when you get there,
// not stubbed for hand-writing like the routes).

// requireAuth — verifies the Clerk session, attaches req.user
