# Gavi411 — Project Brain Repository
_Last updated: 2026-09-01_
_Purpose: running knowledge base for planning the fullstack course final project. Will feed into Claude Code later. Not a project plan yet — that comes after planning is done._
 
---
 
## 1. Context & Constraints
 
- **What:** Final project for Gavi's fullstack course.
- **Requirements:** NOT yet received from instructor. Everything below is based on assumptions and must be reconciled once requirements arrive.
- **Requirements:** React frontend, Node.js backend, PostgreSQL (via Neon), API usage (internal OK), authentication via Clerk, unit testing, CI/CD, GitHub, Jira. Course plan presented and approved as-is (decision #37) — no longer pending official confirmation.
- **Timeline:** ~1 month.
- **Availability:**
  - Job keeps Gavi out of the house 12–13 hrs/day.
  - Class Sun & Tue 18:00–22:45 (includes work time).
  - ~1 hour available on a few other days per week.
  - Effectively: 2 semi-long blocks + a few 1-hr slots per week.
- **Learning goal:** Gavi personally writes at least a section of every kind of piece (a component, a route, an API call, etc.). The rest via Claude agents under his supervision. He must be able to explain everything submitted.
- **Existing skills/context:** SheCodes fullstack course background (Express, SQL, MongoDB, Prisma, React, Git). ES modules. Ubuntu VM dev environment exists from a previous project (Node, Express, Mongoose, Prisma, Claude Code CLI + VS Code extension) — considering reusing it for this one. MongoDB Atlas + Neon accounts. Testing: used Vitest in that previous project; learned Node built-in test runner in class; testing knowledge is weak overall, so whichever framework is chosen will require learning it properly.
## 2. The Concept — Gavi411
 
A webapp digitizing the informal concierge/info-booth/assistance service Gavi already provides to friends & family.
 
**Real-world service examples (from Gavi):** setting up internet/cell plans for relatives moving to Israel; last-minute travel rescue (connections at unfamiliar airports, flight updates before gate announcements, kosher food at airports, maximizing bump compensation); vacation activity planning; product research & selection; directions; opening hours; reaching a person at a company; finding elusive ebooks; middleman purchases (embarrassing or geo-unavailable products).
 
**Current channels:** WhatsApp/Telegram mostly, sometimes calls, sometimes email.
 
### Core feature set (draft, unprioritized)
1. **Request submission** — friends submit requests via the app.
2. **Smart intake/triage chatbot** — replaces a static form. Conversationally gathers all needed info before a ticket is created. NOT an answer-serving bot (decided: answer-serving likely expensive + would duplicate what users can already google, risking annoyance). Triage only.
3. **Ticketing system (admin)** — Gavi sees incoming requests, manages tickets, users, statuses.
4. **Credit system** — anti-abuse / demand shaping / AI-cost control (likely MVP, not v1.1):
   - Each user gets X credits per period (week/month TBD).
   - Possibly different amounts per user tier.
   - ~~Max Y open tickets at any time.~~ Superseded — see decision #59.
   - No rollover.
   - Earn-back mechanic DROPPED: the reward for a closed ticket is the solved problem. Framing is fairness among friends, not gamification/incentives.
   - Side benefit: credits naturally cap AI API call volume.
5. **PWA** (Progressive Web App) — installable on phones via Chrome. Native Android acknowledged as out of scope.
6. **Admin experience** — a full interface for Gavi to review tickets, respond, manage users/statuses. Not merely an "admin mode" toggle on the user UI.
7. **Image support** — users must at minimum be able to send images with requests.
8. **Invite-gated signup** — Gavi sends custom individual invite links; homepage has a "request access" flow approved case-by-case.
9. **Gavi-initiated requests** — from an outside conversation (WhatsApp etc.), Gavi pastes the chat/image into a new request and gets a shareable link:
   - Existing user → gets a notification that a request was opened for them.
   - Non-user → link opens a "one-time" guest chat view of that request, associated with their phone number; if they're later invited and sign up, that history attaches to their account.
10. *(More ideas exist, not yet captured.)*
### Request lifecycle (from 2026-08-08 brainstorm)
- **User actions on own request:** cancel (credit refunded, especially if untouched), "solved it myself" close, and — for urgent requests only — downgrade to "no longer urgent."
- **Urgency:** captured during triage, not a separate user toggle. No promises about response speed anywhere (especially nights/Shabbat).
- **States (draft):** in queue → received → working on it → waiting on friend (clarification OR confirming a solution) → resolved-pending-confirmation → closed. Plus cancelled / self-solved exits.
  - Definitions: **queue** = Gavi hasn't seen it. **received** = seen, not yet handling. **working on it** = actively started.
- **Nudges:** manual nudge by Gavi for stale waiting-on-friend requests. Auto-close undecided.
- **Closing:** Gavi initiates "did this resolve it?" → friend confirms → closed. Post-close reaction: nice-to-have, questionable actionability, parked.
- **Reopening:** closed requests are re-openable, no time limit — a continuation of the same issue shouldn't re-triage.
- **Reminders (new idea):** time-based reminders attached to a request — for Gavi ("check on X at 15:00") or set for the friend ("make sure to X"). Some solutions are time-dependent.
### Service presence (online/offline)
- Gavi can set the service "offline": requests still accepted, but it's clearly communicated responses will wait until he's back.
- Triggers: automatic (Shabbat times) or manual (vacation, busy at work, unreliable internet).
- Deliberately NOT official hours — expectation-setting without a schedule.
- (Note: auto-Shabbat is a fun little feature — candle-lighting times via a free API like Hebcal.)
- Auto-Shabbat requirements: configurable buffer before candle-lighting and after havdala; must handle Gavi being in a different timezone/location (times follow where HE is, not a fixed Jerusalem setting).
### Admin cockpit (to workshop further)
- Sort: by age and by urgency (urgency view defaults oldest-first). Filter: open/closed. Group: by person.
- No canned responses needed.
- **Private notes per request** (not per user) — only visible to Gavi.
### User profiles
- Core: photo, full name, phone, email.
- Optional self-service fields: phone OS/model, computer OS, location, other practical info.
- Philosophy: store the info Gavi *can't* remember (device models change), not preferences he already knows (kosher, travel tastes). These are friends, not CRM contacts.
### User group tags (new, 2026-08-09)
- Users get a group "tag" (tier). Purpose: (1) determines credit amount for that user, (2) usable as an admin sort/filter dimension on requests.
- Relationship to existing "tier" idea in credit system (§ credits mechanics) — likely the same concept; reconcile naming later.
### Credits mechanics (firming up)
- Visible to users. Flat cost: 1 credit per request.
- At zero: "request anyway" available, but only once per reset period.
### UX decisions so far
- **Home screen (user):** list of open requests; button to reveal closed ones; if everything is closed, surface the most recent closed request (info may still be relevant). Prominent "new request" button.
- **Terminology:** the word "ticket" must never appear user-facing. Working term: "request." Better term TBD.
- **Conversations:** live fully in-app. The whole point is consolidation — no bouncing back to WhatsApp.
- **Notifications:**
  - Users: Web Push (PWA notifications) — works on Android/desktop; on iOS only for installed PWAs (16.4+).
  - Gavi: Telegram bot pinging on new requests/messages with a deep link into the admin view. (Telegram Bot API is free and simple — a single POST to send a message.) **Telegram is Gavi-only** — many friends don't have Telegram, so it's never used for user-facing notifications, only Gavi's own alerts.
- **Language/RTL:** ~~UI framework must support RTL + Hebrew from day one (layout direction, text alignment).~~ Corrected by decision #22: UI chrome stays English/LTR only; any freeform text field (input or display, friend-facing or Gavi/admin-facing — request text, messages, notes) needs correct bidi text rendering, not page-layout mirroring.
- **Branding:** no mascot. Logo deferred.
- **Tone:** friendly, almost informal — the app should feel like the service already feels.
### Auth direction — DECIDED: OAuth
- OAuth via a hosted provider (Clerk / Supabase Auth / Auth0 / Firebase — provider TBD), invite-only user base.
- WhatsApp OTP dropped (not zero-budget/zero-hassle: official API needs Meta approval + per-message costs; unofficial libs are ban-risk).
- Gavi prefers NOT to hand-roll secure auth. If the course expects JWT/bcrypt/protected routes: he has demonstrated these in other projects, and they may be woven into other features anyway (e.g., protected admin routes).
## Copywriting Pass (dedicated dev milestone — placeholder strings until then)
Purely wording/phrasing items, decoupled from logic — batched into one pass late in dev since strings can change freely without touching code:
- "Ticket" → user-facing replacement term (currently placeholder: "request")
- Disambiguation prompt wording (draft "This sounds like it could be: X · Y. Which fits best?" rejected as not friendly enough)
- General tone pass across all user-facing copy (friendly/informal per brand direction)
## 3. Decisions Log
 
| # | Timestamp | Decision |
|---|-----------|----------|
| 1 | 2026-08-07 ~AM | Chatbot = smart intake/triage, not answer-serving. |
| 2 | 2026-08-07 ~AM | This doc is the running brain; formal project plan docs come after planning phase. |
| 3 | 2026-08-07 ~PM | Auth = OAuth via hosted provider, invite-only. WhatsApp OTP dropped. |
| 4 | 2026-08-07 ~PM | Unit testing framework = Vitest. |
| 5 | 2026-08-07 ~PM | Credit system leans MVP (doubles as AI cost control). |
| 6 | 2026-08-07 ~PM | Credit earn-back mechanic dropped. |
| 7 | 2026-08-07 ~PM | Admin side = full dedicated interface, not an admin-mode toggle. |
| 8 | 2026-08-07 ~PM | App must support image sending (minimum media requirement). |
| 9 | 2026-08-08 | One DB only — split-by-domain rejected. |
| 10 | 2026-08-08 | Deployment: Vercel + Render acceptable; cold start OK IRL, will pre-warm before demo. |
| 11 | 2026-08-08 | Signup: custom individual invite links + homepage "request access" approved case-by-case. |
| 12 | 2026-08-08 | No "ticket" in any user-facing copy — "request" as working term. |
| 13 | 2026-08-08 | Conversations live fully in-app; no bouncing to WhatsApp. |
| 14 | 2026-08-08 | UI must support RTL/Hebrew from the start; content English-only for now. No mascot. |
| 15 | 2026-08-08 | Urgency set at triage; zero speed promises. |
| 16 | 2026-08-08 | Close flow = Gavi asks "resolved?" → friend confirms. |
| 17 | 2026-08-08 | Requests re-openable indefinitely (no re-triage for continuations). |
| 18 | 2026-08-08 | Notes are per-request, not per-user. |
| 19 | 2026-08-08 | Credits: visible, flat 1/request, one "request anyway" overdraft per reset. |
| 20 | 2026-08-08 | Service online/offline presence state (auto-Shabbat or manual), not official hours. |
| 21 | 2026-08-09 | Tech support confirmed as its own request type. |
| 22 | 2026-08-19 | Correction to #14: UI itself stays English/LTR only — no page-layout mirroring, no logical-CSS sweep of app chrome. Hebrew can appear in *any* freeform text field, input or display, on either side of the app — friend-facing (request text, messages) or Gavi/admin-facing (notes, replies) alike; whoever's typing, not just friends. Those fields need correct bidi text rendering (mixed Hebrew/English/numbers in one string — `dir="auto"` or `unicode-bidi: plain-text` scoped to that field), not the whole page. |
| 22 | 2026-08-09 | Intake trigger config is DB-backed and admin-editable (not hardcoded), for live keyword tuning. |
| 23 | 2026-08-09 | Disambiguation UI = multi-select chips. |
| 24 | 2026-08-09 | User group tag added: drives credit tier + admin sort/filter. |
| 25 | 2026-08-09 | Copywriting is a dedicated later dev pass, not decided inline during feature work. |
| 26 | 2026-08-13 | JavaScript only, no TypeScript, project-wide — Gavi will learn TS after this project. |
| 27 | 2026-08-13 | No real-time chat / WebSockets — message thread + notifications instead. WhatsApp real-time integration ruled out. |
| 28 | 2026-08-13 | Messaging security target: escrowed/conversation-scoped E2E (ECDH + AES-GCM), with admin-side client search and off-server key escrow for recovery. Encryption-at-rest is the fallback if time runs short. |
| 29 | 2026-08-13 | Multi-device recovery model: admin-approved device linking is the primary path (Gavi approves new devices live); off-server escrow is the last-resort fallback for total device loss. |
| 30 | 2026-08-13 | Escrow refined: per-user passphrase (not raw key) generated at invite creation, embedded in invite link fragment, exported via CSV for 1Password at invite time. CSV keyed by name only, no email. |
| 31 | 2026-08-16 | Feasibility check (separate chat): full PRD not buildable hand-written solo in the available time/skill level. Hand-write vs. agentic split defined per feature, with a fallback rule — anything falling behind moves to agentic rather than being cut or rushed. |
| 32 | 2026-08-16 | Multi-agent workflow reconciled with the hand-write split: agents run **alongside** Gavi's commits (triggered by them — e.g. test scaffolding, design/styling passes, CI/CD), not as blocking standalone days. OAuth wiring and E2E encryption remain agentic but are not commit-triggered (they're built in parallel/upfront rather than reacting to a commit). |
| 33 | 2026-08-16 | Testing and CI/CD reclassified as **collaborative**, not fully agentic — Gavi is weak in both and wants to learn them, not just receive finished output. Test scaffolds happen per-feature as each one lands, not batched at the end. |
| 34 | 2026-08-16 | Jira issue template for the Aegis Method finalized — Parent/Child field lists, falsifier and evidence-bar fields placed per SPEC.md, plus a `Role` field on children (Falsifier/State/Diagnosis/ADR/Sandbox/Verifier/Policy/Gate/Follow-up) drawn from real issue-train examples in the Aegis deck (NAP, FTC, BADAS cases), and an Owner/Authorship field tying to decision #31's split. See `gavi411-jira-aegis-template.md`. |
| 35 | 2026-08-16 | Commit convention for Repowise agent-provenance tracking finalized: one git identity (email) + branch prefix + commit trailer per agent role (backend/design/test/e2e/cicd), not per session. See `gavi411-commit-convention.md`. |
| 36 | 2026-08-16 | PRODUCT.md (Impeccable) will NOT be pre-drafted in planning chat — it's generated interactively by `/impeccable init` inside Claude Code, which scans the actual codebase and interviews on 2-3 questions. Deferred to a Claude Code task once the repo exists. |
| 37 | 2026-08-16 | Course requirements resolved: Gavi's plan was presented to the instructor and approved as-is. No further reconciliation needed against official requirements. |
| 38 | 2026-08-16 | DB = PostgreSQL (via existing Neon account), not MongoDB — data is naturally relational (users↔requests↔messages↔credits). |
| 39 | 2026-08-16 | OAuth provider = Clerk. At this project's scale, cost is a non-issue across all four candidates (Clerk/Supabase/Auth0/Firebase all comfortably free-tier); decision made on fit instead. Clerk chosen for DX and prebuilt components; kept decoupled from Neon (already set up) rather than migrating to Supabase to consolidate DB+auth. |
| 40 | 2026-08-16 | Ponytail (YAGNI/minimal-code discipline plugin) adopted, settled in a separate chat. Enforces least-code-that-works: stdlib/native features before custom code or dependencies, no speculative abstractions. Install via `/plugin marketplace add DietrichGebert/ponytail` → `/plugin install ponytail@ponytail` — must be the actual plugin install, since copying SKILL.md into a skills folder does NOT reliably self-activate (confirmed zero activation that way; the plugin wires a SessionStart hook the copy method skips). Requires `node` on PATH for its two lifecycle hooks — fails silently, not loudly, if missing, so worth a one-time check. Given multiple subagents will be committing code, verify the hooks are live in every subagent's context after install, not just the first tested — inconsistent activation would show up as inconsistent code style and skew Repowise's per-agent code-health comparisons. |
| 41 | 2026-08-16 | Correction to decision #31's fallback rule: moving a behind-schedule task to agentic is an **option Gavi can invoke**, not an automatic trigger. Earlier wording implied automatic reassignment; that's wrong. |
| 42 | 2026-08-16 | V2/stretch tier established for PRD v1: **auto-Shabbat detection, reminders, guest view + history merge** — cut from v1 during the 15-day solo feasibility check and never reinstated (unlike Telegram, Web Push, group tags, and E2E, which were explicitly reinstated). **Post-close reaction** reclassified from "Won't" to v2/stretch — Gavi wants this feature, just not in v1; "Won't" wording was misleading. **Tips/donation link (Bit/Paybox)** promoted from a parked half-joke to an explicit v2/stretch candidate — being a joke about monetization doesn't mean it's excluded from the spec, just that it's not a v1 priority. |
| 43 | 2026-08-16 | Dev environment: working entirely in the Ubuntu VM (not split with Windows) — avoids case-sensitivity mismatches with the Linux deploy target (Render) and keeps git identity/tooling config in one place. VS Code can remote into the VM, so no editor experience is lost. |
| 44 | 2026-08-16 | Jira structure: repurpose existing issue types rather than creating custom ones — **Epic → Parent, Task → Child** (no Subtask usage — Aegis children don't nest). The five Aegis states (Open/Implementing/Reviewing/Landed/Reconciled) become the **workflow/status** field, not a custom field — gives the trail for free via Jira's activity log and maps directly onto Kanban board columns. Confirmed working in a **Kanban-style project** (no Scrum/sprint dependency) — fits the flat, freely-reorderable backlog approach already in use. Remaining custom fields to add: Claim, Scope, Assumptions ledger, Falsifier, Evidence required to close, Evidence bar met, Owner/Authorship, Role (children only), Reviewer type. |
| 45 | 2026-08-16 | PRD §9.6 MVP priorities finalized (were "provisional"): **Web Push, PWA installability, User management, Presence control (manual)** move to Must. **Private notes** and **Trigger/keyword admin** stay Should (low priority). **Telegram notifications** stays Should — deprioritized behind Web Push; acceptable to wait if Web Push alone covers notification needs. **Gavi-initiated requests** stays Should. |
| 46 | 2026-08-16 | PRD §9.4 Admin cockpit layout finalized (mobile-first drill-down pattern, not side-by-side): **Screen 1 (list)** — sort/filter/group as a persistent dropdown row at top (not tucked behind a toggle), flat list below sorted by urgency oldest-first by default, each row showing a small WhatsApp-style avatar (initials fallback if no photo), friend's name, request type, short preview, urgency indicator, time since last activity. **Screen 2 (detail)** — header with name/type/status/urgency + back button, segmented tabs for Thread / Details (intake answers) / Notes (private), status-change control pinned near top. |
| 47 | 2026-08-16 | PRD §9.1 Image storage: Cloudinary (free tier), not DB storage — DB just stores the URL. Free tier: 25 credits/month pooled across storage/bandwidth/transformations (1 credit = 1GB storage OR 1GB bandwidth OR 1K transformations), 3 users, no video/advanced-AI on free, soft limit (account suspension risk on sustained overage, not automatic billing). Comfortably sufficient at Gavi411's friends-and-family scale. Also fits the E2E encryption plan better than DB storage, since images already need an "encrypt before upload" step. |
| 48 | 2026-08-16 | PRD §9.2 Credit mechanics finalized: **monthly** reset, **tiered** amounts by group tag (not flat) — Acquaintance: 2/month, Regular: 5/month, Close: 7/month. No rollover, one "request anyway" overdraft per reset period regardless of tier (unchanged from earlier decision #19). |
| 49 | 2026-08-16 | PRD §9.3 Auto-close finalized: a request sitting in **"waiting on friend" for 14 days of inactivity auto-closes**. Before closing, a warning message is sent to the friend notifying them the request will close. Reopening still works per decision #17 (indefinite, no re-triage) — auto-close is a tidiness measure, not a hard end state. **Sending a message in a closed request reopens it** — this is the reopen mechanism itself, not a separate button. |
| 50 | 2026-08-16 | Aegis child-issue authoring rule: Claim, Falsifier, and Evidence bar fields are **not** pre-drafted in bulk during backlog creation — they're written at pickup time, when a child is actually about to move to Implementing, so they reflect real system state (SPEC §5.1 Move 1) rather than speculation against a codebase that doesn't exist yet. Children can be created in Jira with Scope/Role/Owner filled and Claim/Falsifier/Evidence left blank until pickup. |
| 51 | 2026-08-17 | ORM finalized: **Prisma**, on top of the already-decided PostgreSQL/Neon. Fits the relational shape of users↔requests↔messages↔credits and matches Gavi's existing SheCodes/prior-project experience with Prisma. Added to CLAUDE.md's Tech stack list, which had Node/Express/Postgres/Neon settled but hadn't named an ORM explicitly. |
| 52 | 2026-08-17 | **Comment-stub scaffolds for `[You]` tasks** (from an earlier session, not yet written into CLAUDE.md until now): for any task Gavi is writing himself, Claude Code can produce a file with the structure stubbed out as comments (e.g. `// OAuth middleware goes here`, `// connect to DB`, `// GET request - data from DB`) so Gavi fills in the real logic rather than starting from a blank file. Produced **per-task, on request, right when Gavi is about to start that task** — not pre-generated in bulk across the backlog. Applies to **both backend and frontend** `[You]` tasks (not backend-only — frontend component structure/wiring is `[You]` too, per decision #31, and was missing this same courtesy). Because Gavi writes all the real content, the resulting commit is entirely his — **no agent role/identity/worktree involved**, commits under `git-as-gavi` like any other manual work. Distinct from the OAuth-wiring scaffold mentioned in decision #32/47 context, which is genuinely agentic (OAuth wiring itself is `[Agentic]`) — comment-stub scaffolds are a plain authoring aid for `[You]` work, not agent-authored code. |
| 53 | 2026-08-17 | Clerk configured on the **dev instance** (prod instance deferred — Clerk supports adding one later without redoing the dev setup). Sign-in methods: **Google + email/username**. **Phone sign-in dropped** — not available on Clerk's free Hobby plan at all (requires the $25/mo Pro plan, plus $0.01/SMS), and Google+email already cover the friends-and-family user base; not worth a recurring cost for a course project. Phone number will still be **collected as a plain contact-info field** in the app's own `users` table (G411-10) — that's a DB column, not a Clerk auth method, so it costs nothing. **Organizations/multi-tenancy left off** — that's a B2B feature (separate companies/teams each getting isolated orgs); Gavi411 has one admin serving individual friends/family, no multi-tenant concept, so it doesn't apply. Invite-only mode enabled, matching the product's invite-gated model. |
| 54 | 2026-08-17 | **Neon Auth explicitly rejected** in favor of Clerk (already decided/keyed up). Neon Auth syncs Stack Auth-managed users directly into Postgres rows, which is convenient, but is younger/less battle-tested than Clerk, ties auth to the Neon/Postgres choice (Clerk stays DB-agnostic), and has thinner OAuth-provider polish. Enabling both would create two competing systems both claiming ownership of "who is this user" — Neon is used for **Postgres only**. |
| 55 | 2026-08-21 | **`Request.typeDetails` schema + strip-on-save** (G411-23 pickup): TravelFields/PurchaseFields/TechSupportFields (G411-65) collect structured per-type data (dates, budget, device, flight arrays...) that `Request` had no column for. Decided a single `typeDetails Json?` column over per-type relational tables — shape varies per type, Postgres/Prisma JSON handles that natively without a migration per field change; a separate-table approach would be fully relational but with 3x the migration ceremony for no admin-UI benefit at this scale. Compared against "save everything, filter at display time" across clutter/storage/editability/security/debuggability angles — decisive factor was **multiple future consumers** (G411-38's admin detail screen won't be the only reader; an admin API, a CSV export, or Gavi querying the DB directly could each independently need to reimplement "is this empty"). Decision: strip empty/blank values out of `typeDetails` at save time (G411-23's submit handler), so the column only ever holds keys the friend actually filled in — one write-time cleanup beats N read-time filters. Rendering with human-readable labels (reusing the label strings already in each Fields component) is G411-38's job, not this ticket's — G411-23 only owns correct, clean persistence. _(Note: this decision was first written 2026-08-21 but lost to an uncommitted-changes wipe during a same-session `git reset --hard` fix for an unrelated branching slip; re-added 2026-08-23 once the loss was noticed — same underlying lesson as decision #57's git-hygiene note below.)_ |
| 56 | 2026-08-23 | **User profile phone number: first-login gate, not a placeholder-forever pattern.** `requireAuth`'s auto-created `User` row used `phoneNumber: claims.phoneNumber ?? \`pending-${userId}\`` as a stopgap, with nothing ever built to replace it — surfaced by the 2026-08-23 gap analysis (finding C5). The analysis's own suggested justification ("needed before Telegram notifications can rely on real contact data") was **wrong and explicitly corrected by Gavi**: Telegram notifications (G411-50/51) go to Gavi, the admin, not to friends — a friend's phone number is irrelevant to that flow. The actual reason this matters: decision #53 already committed to collecting phone number as a plain app-side field (since Clerk's phone sign-in was dropped for cost reasons), and that commitment was never followed through with an actual UI. Gavi's correction: there should be **no placeholder at all** — collect phone number (and other PRD §3 core profile fields) as part of first-login, before a friend can do anything else in the app, not as a someday-editable settings screen retrofitted around a fake value. Filed as G411-69. |
| 57 | 2026-08-23 | **Reconciled requires the FULL current scope to be landed, not just its original piece** — surfaced when G411-23 (originally just the backend creation endpoint) had `handleSubmit`'s frontend wiring folded into its scope mid-session, and that folded-in work landed on a separate, still-unmerged PR (#4). Gavi caught, live, that Reconciled was about to be confirmed while part of the ticket's own current scope wasn't actually merged yet — correctly blocking it. Fix applied: Jira status corrected Landed → Reviewing (the closest of the 5 workflow states to "blocked on an external PR review," since no dedicated status for that exists) rather than leaving it at a stale "Landed" that implied more was done than actually was. General lesson: when a ticket's scope grows after it first reaches Landed, re-verify what's *actually* merged before treating any later status (Reconciled, or even Landed again) as still accurate — a Landed reached under an earlier, narrower scope doesn't automatically cover scope added afterward. |
| 58 | 2026-08-23 | **Group tag tier name is "Limited," not "Acquaintance."** Decision #48 and PRD §9.2 wrote the third tier as "Acquaintance" — the 2026-08-23 gap analysis (finding C6) flagged this as a mismatch against `schema.prisma`'s `GroupTag` enum (`CLOSE / REGULAR / LIMITED`) and recommended renaming the enum to match. **Gavi corrected this the other way**: the schema was right, decision #48/PRD §9.2 were the stale ones — "Limited" is the actual tier name. PRD §9.2 fixed to read Limited/Regular/Close; no schema change made. Numbers unchanged (2/5/7 per month). |
| 59 | 2026-08-23 | **"Max Y open tickets at any time" (§2 original brainstorm, per-user cap alongside per-user credits) is dropped, not just unfinalized.** Flagged by the 2026-08-23 gap analysis (finding F4) as a concept-section idea that never made it into PRD §6.1's finalized credit table or any Jira ticket, with no explicit decision recorded either way. Gavi confirmed directly: no such limit was ever actually set (the bullet's own placeholder "Y" — never assigned a number, unlike the credit amounts finalized in decision #48 — reflects that it was never a real commitment). Closed as a non-issue: the credit-per-request cost is the sole volume-limiting mechanic going forward, no separate open-tickets cap. §2's original bullet struck through, pointing here. |
| 60 | 2026-08-23 | **Shift back to an agentic-first workflow**, now that the full gap-analysis followup (all 31 findings in `gavi411-gap-analysis.md`/`gavi411-gap-analysis-followup.md`) is resolved — this was the explicit gate Gavi set before starting this shift. Shape: multiple agents working in parallel on more of the backlog, rather than the mostly `[You]`/`[Collab]` pace this session ran at. Two guardrails Gavi wants kept, non-negotiable: (1) **major decisions still come to him before being acted on** — an agent doesn't get to resolve scope/ownership/design tradeoffs unilaterally, same spirit as the existing `[Agentic]` rule ("build fully, but explain fully as you go"); (2) **each completed chunk gets a clear, documented rundown of what was done and how it works — not just a diff** — Gavi wants to actually understand what shipped, not just see a merged PR (matches CLAUDE.md's course constraint that he must be able to explain everything submitted). Mechanics not yet defined — which agents/roles, which tickets move first, how "major decision" gets drawn in practice day to day — to be worked out as this actually starts, not decided in the abstract here. |
| 61 | 2026-08-23 | **GitHub branch protection's "1 approval required" doesn't count an approval from a read-only collaborator** — surfaced live on PR #4. Matan (added read-only per decision-era collaborator policy — review/comment, no push) left two real review comments, both got fixed, then he came back with an explicit `APPROVE THANKS!` comment plus two actual `APPROVED` reviews — but `reviewDecision` stayed `REVIEW_REQUIRED` and the PR stayed `BLOCKED`. Root cause: GitHub's branch protection only honors approvals from collaborators with **write** access, regardless of whether a lower-access user can technically submit an "Approve" review through the UI/API. **Fix**: upgraded both Matan and eldaduz (Gavi's other read-only reviewer) to write access — Matan's took effect immediately (already-accepted collaborator), eldaduz's was applied to his still-pending invitation (`permissions: push` on the invite itself, takes effect once he accepts). Once Matan's *existing* approval was retroactively counted under his new access level, `reviewDecision` flipped to `APPROVED` immediately — no new review needed. **General lesson**: a "review-only, no push" role on this repo needs write access anyway if their approval is meant to satisfy branch protection; read-only is only a real option if approvals aren't required, or if Gavi (as admin, exempt from the rule) is the one merging regardless of their review. |
| 62 | 2026-08-23 | **Agentic-first shift (decision #60) enforcement mechanism, decided before the first pilot launched.** Gavi's stated worry: fully agentic work quietly dropping tests/docs/process steps, discovered only after the fact. Decided **belt-and-suspenders**, not either alone: (1) every agentic child self-runs the same 8-step "wrap it up" checklist (`CLAUDE.md`) before reporting done — scope check, falsifier, Aegis fields, evidence bar, Jira transition, commit, HANDOFF.md, itemized ✓/✗ report; (2) a **live Claude Code Sibling review before merge, on every agentic child** (not just load-bearing ones per the existing narrower policy) — explicitly re-verifying tests exist and pass, docs/HANDOFF.md are actually updated, Aegis fields are actually written, Jira is actually transitioned. Neither step alone is trusted to catch a silently-dropped step; the review exists specifically to catch it if the agent's own self-report is wrong or incomplete. **First pilot**: **G411-66** (sign-in UI gate), explicitly moved from its undecided `[You]`/`[Collab]` ownership to **`[Agentic]`** for this run only — Gavi's explicit call, not an agent's, per the fallback-rule constraint. Chosen as the pilot for being small/contained and already flagged urgent. Run **unattended** (Gavi reviews the full documented rundown once it reports done, not live-monitored) — makes the sibling-review gate the sole real-time backstop, since nothing else catches a skipped step before merge. _(Superseded by decision #63 the same day — the per-ticket `[You]`/`[Collab]`/`[Agentic]` tag this entry still references stopped gating who writes code once the plan actually flipped.)_ |
| 63 | 2026-08-24 | **Correction to decision #60/#62: the plan actually changed, not just "agentic work is now also allowed."** Gavi's own words: "if we're now saying 'there's a new plan' these tags are currently not relevant and should not be used to decide who writes, since the plan is 'agents write.' regardless of the tag. Otherwise we didn't actually change the plan." Decisions #60/#62 still picked G411-66 as a one-ticket "pilot" gated behind an explicit per-ticket `[You]`→`[Agentic]` override — that was still the *old* mode's mechanics (tag decides who writes), just applied once. **What actually changes now**: (1) **`[You]`/`[Collab]`/`[Agentic]` tags on existing tickets are historical record only** — they describe how past work was actually done, they do not gate future pickup. Going forward, an agent writes a ticket by default, full stop; a ticket only stays manual if Gavi explicitly says "I want to write this one" (e.g. to satisfy the course's per-component learning requirement) — that's now the exception called out per-ticket, not something derived from a stale field. (2) **The session-boundary rule (decision from 2026-08-19, `gavi411-commit-convention.md`) flips which mode is the exception.** Under the old plan, agentic dispatch was the occasional add-on and needed isolating from the normal (manual pairing) session. Under agentic-first, agentic dispatch **is** the normal case — a session doing live manual pairing with Gavi is now the thing that, if it needs to also launch agentic work, should do so from a separate tab, not the reverse. The underlying reason for the original rule (subagent tool-call noise makes a transcript unreadable when interleaved with unrelated live pairing) still holds and still argues for *some* separation when both happen close together — but the default assumption inverts. (3) **PR review mechanic corrected to match reality**: Gavi is solo, with no second person available to gate merges — Matan/Eldad were added as reviewers but decision #61 already showed even a real approval from a low-access collaborator doesn't reliably satisfy branch protection, and depending on an outside approval to unblock solo work was never going to hold up. The actual mechanism is: **Claude Code Sibling review (live, before merge) is the review — self-merge once it passes**, same shape routine `[You]`/`[Collab]` children already used via the evidence bar, just extended to be the universal gate rather than something bolted onto specific "load-bearing" categories. This was already implicitly the plan in #62's "Sibling review before merge" language — #63 just removes the leftover assumption that an outside human approval was ever required on top of it. |
| 64 | 2026-08-24 | **Historical finding, corrected same day: roughly the first third of this project's git history was committed directly to `main`, but not because branch protection was bypassed.** `git log --first-parent main` shows 70 total commits, only 16 of them merges — the first merge commit is `bfcc197` (G411-11); everything before it (initial setup, `gavi411-brain.md`/PRD authoring, G411-10's schema design, and even the commit that *introduced* the branch-per-child policy itself, `51d01be`) was pushed straight to `main`. **Original write-up wrongly claimed `main`'s branch protection (with its admin exemption, `enforce_admins: false`) was already in place and silently bypassed — Gavi corrected this: branch protection was only added after PR #4**, well after this direct-commit period. The real cause is simpler: earlier Claude Code sessions just never switched off `main` before committing, no rule-bypass involved, nothing exotic. Recorded as historical fact, not corrected retroactively (rewriting merged history for a solo course project has no real upside). **Forward-looking**: `enforce_admins: false` is still real and still means Gavi's own pushes bypass the PR-approval requirement today — worth being deliberate about, not something to lean on, but it isn't what explains this particular finding. |
| 65 | 2026-08-24 | **Known gap: Repowise's per-role provenance tracking hasn't actually been fed since the agentic-first shift started.** Gavi asked directly whether worktree/identity mechanics were checked as part of the agentic-first correction — they hadn't been. Checked live: **G411-66** ("first agentic-first pilot") was built and merged (PR #8) without touching the persistent `Gavi411-agent-backend` worktree at all — that worktree's last commit is still 2026-08-19 (5 days stale at the time of checking), and the actual merge commit is attributed to Gavi's own GitHub account (`73096923+GaviLazan@users.noreply.github.com`), not `agent-backend@gavi411.local`. Root cause: the background `Agent` tool doesn't automatically `cd` into a persistent role worktree or run a `git-as-agent-*` identity switch — those have to be spelled out explicitly in the launch prompt every time, and weren't. **Not retroactively fixed** — G411-66 and G411-72's commits stay as merged/attributed; rewriting merged history for a solo course project has no real upside, and the code itself is correct and Reconciled. **What this means going forward**: if Gavi ever runs Repowise's own attribution reports, they will undercount/misattribute agentic-authored work done during this gap (roughly G411-66 through G411-72, 2026-08-24) as manual/GitHub-account work rather than `agent-backend`'s. Fix applied prospectively in `gavi411-commit-convention.md`'s launch checklist (explicit prompt instructions + a post-hoc identity/worktree verification step) so it doesn't keep happening — see that file for the mechanism. |
| 66 | 2026-08-24 | **Corrected live on G411-67 pickup: a stale `[You]`/`[Agentic]` tag on a ticket is not grounds to stop and ask which mode to use.** A session checked G411-67's ownership tag, found the old split-owner pattern, and used `AskUserQuestion` to ask Gavi which mode to run it in — Gavi corrected this immediately: decision #63 already settled agentic-first as the standing default for the whole current period, not a per-ticket approval gate to be re-litigated every time an old tag shows up. Finding a stale tag (or a HANDOFF.md/ticket mismatch) is worth one line of mention, but re-opening "which mode" as a live question defeats the point of having a standing default. `CLAUDE.md`'s Ownership Split section updated with this correction inline, so a future session reads it before repeating the mistake rather than only finding it in this log. |
| 67 | 2026-08-24 | **A question buried mid-paragraph while tool calls keep running is not a real stop-and-ask — it has to be a genuine blocking pause, every time.** Caught live, twice in one session: (1) the G411-63 build agent hit a real spec ambiguity ("what counts as 'closed'?" for the request-list toggle), picked the broader reading itself, and only documented the choice in the PR/Jira description for later review — Gavi answered it correctly once he happened to spot it buried in a long turn, but the code was already written by the time he could respond; (2) the orchestrating session itself repeated the same pattern conversationally — asking something in passing inside a longer message while continuing to work, so by the time Gavi caught and answered it, several more tool calls had already run past the point where the answer would have changed anything. Gavi's own framing: he can't realistically catch every question buried inside fast-moving turns, so the *mechanism* has to guarantee the pause, not his read speed. **Fix**: genuine ambiguity (scope, a spec gap, "which of two defensible readings") must go through `AskUserQuestion` — a real blocking pause, no further tool calls on that thread of work — never a note-and-continue or "pick the defensible option and flag it in the final report." Applies to unattended subagents too: if the dispatched agent can't call `AskUserQuestion` itself, it hands the question back to the orchestrating session to ask, rather than resolving it unilaterally. Logged as a concrete mechanism under `CLAUDE.md`'s Required workflow, checkpoint 3 (mid-flight stop), not just here — the doc needed a "how to actually stop," not just a restated "stop when unsure." |
| 68 | 2026-08-25 (per Gavi, originally discussed 2026-08-24) | **Merge policy: always a regular merge commit, never squash — standing rule.** Gavi says this was discussed in the 2026-08-24 session, but a full repo search (`gavi411-brain.md`, `gavi411-commit-convention.md`, `CLAUDE.md`, every other doc, and every commit message from that date) turns up no record of it — PRs #4, #8, #9, #10 were all squash-merged without objection, and the commit-convention file's own merge-cleanup section (added 2026-08-24, same session) talks about "a manual squash" as the expected case, not something to avoid. **Recorded as a real gap**: either the instruction was given verbally/elsewhere and never made it into a doc (the actual failure — an undocumented standing rule is indistinguishable from no rule at all, the next session has no way to know it exists), or it's being set as a new rule now under the belief it was already settled. Either way, **effective now: every PR merge uses a regular merge commit** (`gh pr merge --merge` / `git merge --no-ff`), not `--squash`. Branch-cleanup-on-merge (decision recorded in `gavi411-commit-convention.md`, 2026-08-24) still applies regardless of merge strategy — delete the branch immediately after, both remote and local. Caught live: G411-74/PR #21 was squash-merged before this was raised; not retroactively rewritten (same reasoning as decision #64 — no real upside to rewriting merged history on a solo course project), corrected going forward starting with the next PR. |
| 69 | 2026-08-25 | **G411-64 build round: several real, reusable decisions surfaced through live visual review, not just ticket-scoped fixes.** (1) **Zero-match on the intake flow now lands on the chip screen** (full 5-type list, no "None of these" chip since there's nothing to reject) **instead of skipping straight to the GENERAL follow-up** — reverses the original G411-18/21-era call ("zero match → skip chips, land directly on GENERAL"). Gavi's direct correction: skipping chips removed a real choice point with no upside. (2) **Discard/exit confirmations use a real in-app modal, not the native browser `confirm()` popup** — new shared `ConfirmModal.jsx` component (native `<dialog>`, no library), used by both the intake flow's "×" and the header logo's confirm-if-typed guard. Standing pattern going forward: any future are-you-sure prompt in this app should use this component, not `confirm()`/`alert()`. (3) **`box-sizing: border-box` is now set globally** (`*, *::before, *::after` in `index.css`) — root-caused two separate-looking layout bugs (home-page width mismatch, intake-card clipping) back to one shared cause: nothing in the codebase set border-box before, so any element mixing padding + a `max-width` silently rendered wider than intended. Worth knowing for any future component: padding + explicit width is now safe by default, doesn't need a manual box-sizing override per component. (4) **`scrollbar-gutter: stable` added to `html`** — a real (non-headless-browser) scrollbar reserving/releasing its width between a tall page (the home request list) and a short one (an intake card) was shifting the whole centered `#root` column horizontally between screens; this reserves the gutter unconditionally so page height never changes available width. (5) **A two-card "outgoing slides out, incoming slides in, simultaneously" step-transition was built, then reverted** after repeated real bugs across several fix attempts (a stale-closure cleanup timer that never fired, a CSS specificity conflict producing a vertical "pop," and a real-browser-only case where an animation class present on an element's very first painted frame sometimes never visibly played) — Gavi's call to fall back to the original single-card fade+slide-in once the added complexity kept producing new bugs faster than it was fixing old ones. **Recorded so this isn't re-attempted blindly**: if a genuine simultaneous slide-out/slide-in is wanted later, budget real time for cross-browser animation-timing edge cases, not just headless-Chromium verification — headless testing did not reproduce any of these bugs on its own. |
| 70 | 2026-08-27 | **"Reconciled" is overloaded when a ticket's original premise turns out wrong — the workflow has no Cancelled/Won't-Fix state, so "the original question was moot" and "the real work is actually done" collapse into the same terminal status.** Concrete case: G411-71 originally assumed Clerk's native Invitations API was the intended invite mechanism; at pickup (2026-08-23) that assumption was found wrong and corrected in the ticket's own text ("Clerk's own invite-sending was never the plan"). But the ticket's real remaining scope after that correction — decide + implement the actual invite-gating mechanism (two options laid out explicitly, Falsifier + Aegis note both saying "at pickup") — was never done, and the ticket still shows **Reconciled**, with no code implementing either option (confirmed live, no invite-token validation or Clerk allowlist calls anywhere in `server/` or `client/src/`). A later user-journey walkthrough (`gavi411-user-journey-walkthrough.md`) flagged this as a "Jira-status-vs-reality mismatch," which Gavi correctly pushed back on: the walkthrough had in fact read the ticket's full text (confirmed by re-checking its output), but treated "Reconciled + no code" as only interpretable as a mismatch, without stepping back to ask whether the status was doing double duty — closing out a killed original premise **and** implicitly (wrongly) signaling the pivoted remaining scope was done too. **Fix, going forward**: when a ticket's originally-assumed premise is corrected mid-life (an explicit "Correction" section, same pattern as G411-71/G411-77's history), the correction being resolved and the ticket's *actual* (possibly pivoted) remaining scope being done are two separate facts — don't let a single terminal status stand in for both. Options for a future session hitting this: reopen and re-scope explicitly rather than leave a stale Reconciled sitting on unfinished pivoted work, or add a Jira comment (not just description prose) making clear which part is closed and which remains — visible in the issue's activity trail, not just buried in a Description edit. G411-71 itself: disposition (reopen vs. leave as-is, scope destination) still pending Gavi's review as of this decision being logged — not yet actioned. |
| 71 | 2026-08-27 | **What actually counts as a "gap" — corrected after the user-journey walkthrough (`gavi411-user-journey-walkthrough.md`) mislabeled most of its own findings.** Of 17 items the walkthrough reported, Gavi rejected 12 on review: a ticket/Epic being Open with real scope not yet built (admin cockpit, lifecycle logic, Web Push, presence toggle, credits, encryption-at-rest, a second-admin UI) is **not a gap** — it's just the backlog, on a project that is not half-built yet, let alone shipped. A gap analysis (or user-journey walkthrough) exists to find two specific things: (1) something a *currently in-progress or about-to-be-built* piece of work actually depends on that doesn't exist anywhere — a real blocker, not a future nice-to-have; (2) a **status/reality mismatch** — a ticket marked Reconciled/Landed whose claimed work doesn't actually exist in the live code. Neither of those is "this Open ticket's feature isn't built yet," which is simply what Open means. Root cause of the mislabeling: the walkthrough (and the orchestrating session presenting its findings) treated "not yet built" as inherently worth reporting once found, rather than filtering it against "is anything currently depending on this" before calling it a gap — a scoping failure in how the analysis defined its own terms, not a fact-finding error (the underlying grep/code-read facts in the walkthrough were accurate; the judgment about what they meant was not). **Standing rule going forward**: before any future gap-analysis/walkthrough reports a finding, it must satisfy at least one of the two conditions above — cite the specific in-progress work it blocks, or cite the specific claimed-done ticket it contradicts. "Feature X isn't built and its ticket is Open" is not, on its own, a valid finding. Of the 17 original items, only 3 held up as real (invite-gating mechanism, profile-completion urgency, a missing `@updatedAt` schema directive) plus one reframed from "gap" to a build-time implementation reminder on its own not-yet-picked-up ticket (new users must get group-tag-tiered credit on creation once G411-45 is built) rather than filed as a problem today. |
| 77 | 2026-08-30 | **Invite-gating mechanism decided: app-side token validation, not Clerk allowlist** (G411-81, the ticket that decided this). Clerk's native allowlist needs a known email address at invite-creation time, which G411-41's invite design deliberately doesn't require (Gavi may not know a friend's eventual sign-in email, or they may sign up with a different one than expected). App-side validation (`requireAuth` gates new-`User`-row creation on a valid, unused invite token) works regardless of which email a friend eventually uses. |
| 78 | 2026-08-30 | **A one-shot claim on a row with a foreign-key column can't be a single atomic write if the FK target doesn't exist yet — needs a two-phase claim.** Discovered live on G411-81: the first atomic-claim fix (closing a TOCTOU race that let two concurrent signups both pass a read-only validity check before either claimed) moved the exclusivity claim to *before* `user.create`, but `PendingInvite.usedByUserId` is a foreign key to `User.clerkId` — writing it before the User row exists violates the FK, a real crash caught only by Gavi's own live retest, not by any automated check. **Fix, generalizable pattern**: split into `claimInvite()` (sets only `usedAt`, no FK, the actual exclusivity gate) → create the real row → `linkClaimedInvite()` (fills in the FK-bearing field once the target exists) → `unclaimInvite()` (releases the claim if the row-creation step fails afterward, so a failed signup doesn't permanently burn the invite). Worth remembering for any future one-shot-claim-then-create flow in this codebase. |
| 79 | 2026-08-30 | **A Sibling review's findings can be reviewing stale/cached code, not the real current state — verify against the actual pushed commit hash before trusting a "still broken" finding.** G411-81's third Sibling review pass returned findings that looked like real regressions but were confirmed (via `gh pr view --json headRefOid` matching what was actually pushed) to be re-flagging already-fixed code the review had somehow read stale. Treated as a false positive, not actioned — but recorded as a real failure mode of the review process itself: a review result contradicting recent fixes is itself a signal to check the commit hash before assuming the fix regressed, not just to re-fix blindly. |
| 80 | 2026-08-30 | **The `.cl-footerAction` CSS-hide of Clerk's own "Sign up"/"Sign in" footer link is non-exploitable, confirmed and left as-is.** Raised as a possible gap during G411-81 (a visible `href` in devtools could theoretically reach Clerk's hosted sign-up regardless of the CSS hide) — checked and closed: every data-mutating route independently goes through `requireAuth`'s same invite-token gate regardless of which URL/link path reaches Clerk sign-up, so bypassing the CSS-hidden link grants no additional access; the gate lives at account-creation time, not at UI-visibility time. Gavi's call to leave the CSS-hide as cosmetic-only, no further hardening needed. |
| 81 | 2026-08-31 | **G411-28's own ticket text had a real scope hole neither it nor any other ticket covered — found live during stage-4 pickup, not pre-known.** G411-28's description explicitly excludes wiring live messages into E2E crypto, pointing at G411-27 ("the encryption-at-rest fallback... is a separate task") — but G411-27 is the unrelated **server-side, at-rest** fallback (key in an env var, plaintext still visible to the server), not client-side E2E. Meanwhile the real, live `Message` thread (`POST/GET /:id/messages`, `MessageThread.jsx`, `RequestDetail.jsx`) was 100% plaintext, and stages 1-3's crypto core (`crypto.js`/`keyStore.js`) sat unwired the whole time (decision #28's "standalone subsystem" framing, correct at the time but never revisited). **Filed G411-82** to own that wiring, parented under the Messaging epic, in normal queue order — not folded into G411-28 or jumped ahead, per decision #71's own discipline about what counts as a real gap vs. just backlog. Caught by Gavi's direct prompt to walk the actual user journey rather than trust the plan as scoped. |
| 82 | 2026-08-31 | **G411-28 stage 4 narrowed to escrow-only for this pass** — search index and device-linking (the other two pieces in stage 4's original scope) need real encrypted messages to operate on, which didn't exist until G411-82 landed. Building them against synthetic/no data would have been infrastructure theater, not a real feature (confirmed independently by the build agent's own read before this was even raised). Escrow stood alone correctly since it escrows key material, not message content. |
| 83 | 2026-08-31 | **Keypair generation stays exactly where escrow already puts it — at invite-claim/signup time only — not moved to a general sign-in bootstrap for existing/keyless accounts.** Escrow's whole mechanism depends on the passphrase, which only ever exists in that one signup moment (embedded in the invite link's URL fragment); a keypair generated later at an arbitrary sign-in has nothing to escrow it against. Gavi's call, given real users/messages/requests get purged before go-live anyway — not worth building throwaway migration code for test/dev accounts that won't exist post-launch. The one gap this left (a signup with no passphrase at all — stale link, stripped fragment — got no keypair either, since escrow was the only code path that ever called keygen) was closed as part of G411-82: `createAndUploadKeypair()` now covers the no-passphrase case too, so every real signup ends up with a keypair one way or the other. |
| 84 | 2026-08-31 | **Standing rule: post the Sibling review + fix conversation as real PR comments, not just chat.** Gavi's explicit ask — every review pass and every fix pass that follows gets its own `gh pr comment`, so a PR reads as an actual back-and-forth anyone can read later, independent of any particular chat session. Added to `CLAUDE.md`'s process section (rule 5, alongside the existing Sibling-review requirement) rather than left here only, since it's a standing workflow habit every future session needs to follow by default, not a one-time judgment call. First applied on PR #34, including a retroactive backfill of that PR's first 3 review/fix rounds. |
| 85 | 2026-08-31 | **`main`'s branch protection (`required_approving_review_count: 1`) has now blocked 2 separate PRs (#33, #34)** despite the merging account being a repo admin with `enforce_admins: false` — which should exempt admin merges per GitHub's own model, and matches how 32+ earlier PRs merged cleanly with zero actual reviews. Root cause not identified either time (checked every axis the API exposes — settings, account permissions, `isAdminEnforced` — all identical to the successful merges). Gavi's explicit call both times: bypass via `gh pr merge --merge --admin`, never on the session's own judgment without asking first. The setting itself is still unchanged; worth someone actually digging into the repo's branch-protection config (not just the API's summary view) if a third PR hits this — two occurrences is a pattern, not noise, even though the cause is still unknown. |
| 86 | 2026-08-31 | **Real, live risk found while doing G411-82's Reconciled verification: no account on the real system has an escrow-protected keypair, including Gavi's own admin account, and there's no way for admin to help another keyless account recover.** Confirmed live via a direct DB query: every `User` row's `publicKey` was null, including admin's, until Gavi manually used `InviteAdmin.jsx`'s "Generate my encryption key" button — but that button calls the plain, non-escrow keygen path (no `PendingInvite` token exists for an admin account to escrow against), so admin's key now exists with **zero recovery path**: if Gavi loses his device, every encrypted message he's party to is gone, permanently, no CSV, no passphrase. Separately, a real pre-existing account (Gavi's cousin, shown the app live before G411-82's keygen-at-signup wiring fully landed) is stuck the same way with no self-service fix. **A private key can only be generated on the device that will hold it** (Web Crypto's `generateKeypair()` is client-side by design, the private key never leaves the browser) — so "admin generates a key for someone else" is architecturally impossible; the real fix has to be admin flagging/nudging a keyless account, with the actual keygen still happening in that user's own signed-in session. Filed as **G411-83**, parented under the Messaging epic — not blocking G411-82's own Reconciled (G411-82's actual scope, wiring live send/receive to real encryption, is independently complete and verified), but a real, separate, currently-live gap in account/key lifecycle that needs its own fix. |
| 87 | 2026-09-01 | **G411-28 and G411-83 briefly read as two competing key-recovery mechanisms — resolved before any code was written, per Gavi's explicit direction.** G411-28's own long-standing spec (predates G411-83) already described admin-approved device-linking as the real, permanent multi-device architecture (a `Device` model, request/approve, admin re-encrypts existing content to a new device's key — Signal/WhatsApp-style). G411-83 (decision #86, filed the day before) was drafted without weighing that existing plan and proposed a same-device-only self-service fix instead. **Gavi's call**: G411-28's device-linking is the true, permanent scope; G411-83 is downgraded to a narrow bootstrap patch on top of it — covering only accounts that predate the whole system (no keypair, no device history to bootstrap from), his own admin account's separate self-escrow gap (single-device backup-of-last-resort, not a multi-device transfer, so genuinely a different mechanism from device-linking), and admin-visibility into who's stuck. Both tickets' Jira descriptions rewritten same-day to state this relationship explicitly and cross-reference each other, so neither reads as competing or duplicated work going forward. **Also decided the same session**: device-linking's "admin gets notified" step does not get a real notification channel yet — that's the unbuilt Notifications epic's job (G411-29/49/50/51, G411-7) — tonight's build only stubs one named call site (`notifyAdminOfDeviceRequest()`) so that epic has an obvious place to wire a real notification later instead of rebuilding the trigger point from scratch. The pending-requests list itself is a real DB-backed query regardless (not notification-dependent) so G411-37/38's admin cockpit can plug straight into it later. |
| 89 | 2026-09-01 | **Matan's real Sibling review on PR #35 (G411-28) found two High-severity correctness bugs neither Gavi nor the automated review round caught — walked through live with Gavi via a flowchart of the actual mechanism, fix plan decided together.** (1) A linked device's conversation keys are wrapped only once, in a batch, at approval time — any Request created afterward has no wrap, so `getConversationKey()`'s ECDH fallback silently derives a valid-but-wrong key (the linked device's own keypair was never exchanged with the other party), corrupting that conversation permanently with zero error surfaced. Fix: both (a) and (b) together, not either alone — (b) `getConversationKey()` returns `null` instead of the wrong key when a linked device has no seeded wrap for a request (fails loud via the existing `needsKeypair` path); (a) a sweep re-wraps missing conversations, triggered on **every app load/mount** (not "on sign-in" — a PWA install may never re-trigger that event in practice) plus on admin opening a specific Request's detail page (covers a long-lived open tab). (2) The "request access" recovery button conflates two different causes of `needsKeypair` (this device has no key vs. the other party has no key) and always destructively overwrites this device's private key regardless of which is true — fix is to distinguish the two causes and only offer/trigger the destructive path for the first. **Deferred, filed as G411-84**: a genuinely real-time version of (a) using Web Push to wake admin's PWA's Service Worker and do the wrap in the background, even while admin is offline/asleep — confirmed against real platform constraints (iOS/Android both require a push to show a visible notification, true silent push isn't permitted, but the actual background work can still run independent of whether admin taps it) — not buildable until G411-29's Web Push infra exists, and the Service-Worker-can-safely-run-this-crypto question needs real testing, not assumed. G411-28's own sweep-based fix doesn't block on G411-84 ever landing. |
| 90 | 2026-09-01 | **Standing rule: always use `gh` for GitHub operations, never the `github` MCP connector — full stop, not just when it happens to be down.** Real gap caught live on PR #35: the required Sibling-review-findings-as-PR-comments step (rule 5, `CLAUDE.md`) was skipped in favor of only printing findings in chat, because the `github` MCP connector had failed to connect and `gh` — already authenticated, already the tool every merge/branch-cleanup step in `gavi411-commit-convention.md` already uses — was never tried as the obvious fallback. Gavi's direct correction: this project's repeated "GitHub problems" trace back to retrying the MCP instead of just using `gh`, which has never actually failed here. Documented in `gavi411-commit-convention.md`'s PR-review section so it's not just a private habit — don't even attempt the MCP first "to check," go straight to `gh` for every GitHub op in this project. |
 
## 3a. Known critical gaps — not yet fixed, must be

**Clerk↔Prisma user sync + admin role, flagged crucial by Gavi (2026-08-25).**
Not investigated or fixed yet — this is a flag, not a decision. Gavi's own
words: "I don't think clerk and the db is the right implementation, or if
it is, we didn't do it right. I feel like we have two user lists we are
trying to sync and the data we need from clerk isn't passing into prisma.
We also need to define an admin account which we haven't defined (fixable,
but since we aren't seeing which user we're promoting that's a major
problem)."

Real code-level evidence backing this up, found while filing the flag
(`server/middleware/auth.js`, `requireAuth`):
- **Two separate user stores, one-way sync only, on first touch**: Clerk's
  own user directory and Prisma's `User` table are distinct. The Prisma row
  is lazily created on a user's *first* authenticated request and never
  updated again — the code's own comment admits this (`ponytail: no Clerk
  webhook sync (profile edits in Clerk won't propagate here after
  creation)`). A user who changes their name/email in Clerk after their
  first request has permanently stale data in the app's own DB.
- **Clerk claims likely aren't populated the way the code assumes**:
  `requireAuth` reads `claims.firstName`/`claims.lastName` off
  `req.auth?.sessionClaims`, but Clerk's default session JWT does not
  include `firstName`/`lastName` unless a custom JWT template explicitly
  adds them — nothing in this repo currently configures one. If that's
  right, every new user row is silently created with empty-string names,
  which would explain "the data we need from clerk isn't passing into
  prisma."
- **No admin-role mechanism exists**: `role === 'ADMIN'` is referenced in
  `GET /api/requests` (G411-67, per its own HANDOFF notes) but nothing
  anywhere sets a `User.role` to `ADMIN` — no promotion endpoint, no
  seed/manual-set path, and (Gavi's specific point) no visibility into
  which Clerk/Prisma user *is* Gavi's own account to even promote
  correctly by hand.
- Likely other issues in this area not yet enumerated (Gavi's own words:
  "there are other issues there too").

**Filed as G411-76** (Open, parented under G411-5 Admin Cockpit, verified
via JQL) — description carries this same writeup. Real
investigation/scoping (confirm the JWT-claims theory, decide the actual
fix: webhook sync? different claims strategy? a one-time admin-promotion
script vs. a real UI?) still needs to happen at pickup, not pre-decided
here. Flagged here and in `HANDOFF.md` so it isn't lost — Gavi called this
crucial, not a someday-nice-to-have.

## 4. Open Questions
 
- User-facing term to replace "ticket"/"request" — brainstorm later.
- Notification stack: Web Push for users (+ iOS caveats), Telegram bot for Gavi — confirm in planning.
- Chatbot: LLM-powered (Claude API pay-as-you-go) vs rule-based semi-smart triage vs hybrid. See §7 cost notes.
- PWA: in MVP or stretch goal? (Very low effort — see §7.)
- Image handling: storage approach (DB vs object storage e.g. Cloudinary free tier) — for planning phase.
- Jira + GitHub workflow structure — Parent/Child template now defined (see §3 #34); Jira project itself not yet created.
- What "CI/CD" needs to mean for this project — now reclassified as collaborative (decision #33); Gavi still needs the conceptual explainer during that work.
- PRODUCT.md (Impeccable) — confirmed staying deferred to `/impeccable init` inside Claude Code once repo exists (decision #36). No PRODUCT.md draft exists or should exist from this planning chat — any earlier kickoff-doc instruction to pre-draft it is superseded.
## 5. Parking Lot (ideas mentioned, not yet explored)
 
- Gavi has more feature ideas not yet written out.
- User tiers.
## 6. Reference Notes (from planning discussions)
 
### Real-time chat vs. message thread (2026-08-13)
- Teacher raised "real-time chat" as something to scope carefully.
- DECIDED: not a real-time system (no WebSockets) — free Render tier can't hold persistent connections well (same spin-down issue as elsewhere), and it's not needed.
- What's actually built: a message thread per request — fetch on load, POST to send, notifications (Web Push for friends, Telegram for Gavi) tell people to come check. This satisfies "I respond, they reply" without real-time infrastructure. Frame this explicitly to the teacher as the scope boundary.
- WhatsApp integration for real-time: ruled out — same reasoning as WhatsApp OTP earlier (official API needs Meta approval + per-message cost; unofficial libs are ban-risk). Not pursuing.
### Message encryption (2026-08-13) — DECIDED: conversation-scoped E2E, admin-side client search, off-server escrow
- Target design (stretch, agentically implemented; Gavi needs to understand/explain it, not hand-write it):
  - Each user (friends + Gavi) generates an asymmetric keypair client-side via the browser's Web Crypto API on first use. Private key stays in the browser (IndexedDB); public key stored openly on the server.
  - Each 1:1 conversation (always exactly friend + Gavi — no group-chat complexity) derives a shared secret via **ECDH** from the two parties' keys. Messages encrypted client-side with **AES-GCM** using that secret before ever reaching the server. Images get the same treatment (encrypt before upload, decrypt on display).
  - Server stores/relays ciphertext only — genuinely unreadable to it. Protects against DB leaks/backups AND full server compromise (unlike encryption-at-rest, where a full server compromise exposes the key too).
  - **Admin-side search solved**: Gavi is a legitimate party to every conversation (always one of the two keys). The admin panel can derive shared secrets for all conversations and build a local, client-side decrypted index to search — same trick WhatsApp uses for "search all my chats" despite E2E. This is NOT server-side search and doesn't compromise the server's blindness.
  - **Key-loss problem (friend wipes browser data) solved via escrow**: per-user escrow (not one shared key — a shared key would mean one leak exposes everyone; per-user contains a leak to one person). Design detail: what's escrowed is NOT the raw private key — it's an encrypted backup of it. Flow:
    1. Gavi creates an invite (existing case-by-case flow) → admin panel generates a random escrow passphrase at that moment, before the friend does anything.
    2. Passphrase embedded in the invite link's URL **fragment** (`#escrow=...`) — never sent to the server, same trick as recovery links.
    3. Gavi exports the passphrase to a CSV immediately at invite-creation time (see export feature below) — doesn't depend on friend completing signup.
    4. Friend clicks invite, signs up. Browser generates real keypair, reads the passphrase from the link fragment, uses it locally to encrypt a backup of the private key, uploads only that ENCRYPTED BLOB to the server (unreadable without the passphrase), then discards the passphrase — friend never needs to know it exists.
    5. Server stores the encrypted blob freely (safe — useless without the passphrase). Gavi holds the only usable copy of the passphrase, off-server, in 1Password.
  - **Escrow export feature**: on invite creation, a CSV download for 1Password import — columns `title, username, password, notes` (1Password-friendly, manually mappable): title = "Gavi411 escrow — <friend's name>", username = left blank (no email — invites aren't tied to a pre-known signup email, so name is the only reliable identifier at invite time), password = the escrow passphrase, notes = generation date + "Gavi411 recovery key — do not share." Gavi deletes the CSV from Downloads after import (Downloads not cloud-synced, confirmed low risk).
  - Threat model: this is technically "escrowed/recoverable E2E," not pure E2E (since Gavi can restore any user's key) — but that's correct and intended given Gavi is meant to see all messages by design. The critical detail making it worth building over plain encryption-at-rest: the escrow key must live OFF the server. If it lived server-side, a full server breach would expose everything, collapsing this back to equivalent-to-EaR.
  - Comparison locked in:
    - **Encryption-at-rest (fallback)**: simple (encrypt/decrypt column with a server-side key via Node's `crypto`), full server search, but a full server compromise exposes everything (server holds data + key together).
    - **Escrowed E2E (target)**: DB-only leak AND full server compromise both stay protected (attacker would need to separately compromise Gavi's password manager); full admin search preserved via legitimate client-side decryption; friend data-loss solved via off-server recovery link. Cost: meaningfully more moving parts (keypairs, ECDH, escrow flow, recovery mechanism, local search index) than EaR.
  - Plan: attempt escrowed E2E once the core app works; encryption-at-rest is the guaranteed v1 fallback if time runs out.
### Multi-device model (2026-08-13, refinement of E2E design)
- New device (or new browser/PWA context) has no local key — this is expected, not broken. Login (OAuth) always works regardless; it's identity/access/the public-key registry, separate from per-device message-decryption ability.
- **Primary recovery path: admin-approved device linking.** Friend opens Gavi411 on a new device, logs in, sees "request access to your message history." Gavi gets notified, approves via admin panel. Gavi's admin session already holds decrypted content for that conversation (Gavi is a party to it) and re-encrypts/shares what's needed to the new device's public key. Server only ever relays ciphertext. Modeled on WhatsApp's QR device-linking handshake, adapted since Gavi (not another user device) is the one who can approve.
- **Escrow (see above) = last resort**, for when there's no already-approved device left at all (e.g. lost every device) — no live device to approve from, so the off-server recovery-key/link flow is the only remaining path.
- Adding a device is additive, not a swap — old devices keep working; multi-device is normal (same model as Signal/WhatsApp).
- Future (v2-of-v2, not urgent): explicit device revocation for lost/stolen phones — removes that device's key from receiving future messages. Doesn't undo past decryption on that device.
### Claude API cost reality
- The API is pay-as-you-go and separate from claude.ai subscriptions — no Pro plan needed. You add a small amount (e.g., $5) to a Claude Platform account and it lasts.
- A triage conversation (short, bounded, using a small model like Haiku) costs fractions of a cent. Dozens of friend-tickets/month ≈ well under a dollar.
- Real risk isn't cost per call, it's unbounded usage — mitigated by credits, invite-only user base, and message caps per triage session.
### Intake v1 design (2026-08-08/09, refined)
- **Free-text box** always present: "tell me what's going on."
- **Debounced keyword matching** (wait ~400ms after typing pauses, then check) against a trigger list stored in the DB (not hardcoded) — editable live from an admin screen, no redeploy needed to tune keywords.
- **Matched type(s)** reveal relevant follow-up fields. Multiple matches → disambiguation UI: multi-select chips (DECIDED, 2026-08-09 — supersedes earlier single-select-plus-"both apply" idea; submit-click on mobile judged acceptable). Wording is a copywriting-pass item (see below), not decided now.
- **Info/Research type has no follow-up fields** — free text + the generic fallback question is enough for that category (lighter-weight than other types).
- **Generic fallback field** always shown regardless of match: "anything else I should know, when do you need this by."
- **Starter keyword taxonomy (draft, to refine from real submissions):**
  - Travel: flight, airport, layover, connection, gate, boarding, terminal, trip, vacation, hotel, itinerary, visa, luggage, baggage, delayed, cancelled flight, rebook, compensation, bumped
  - Tech support: phone, sim, sim card, plan, cellular, wifi, wi-fi, internet, router, setup, set up, laptop, computer, app, account, login, password, sync, connect, device, upgrade, update, install
  - Purchase/Find: buy, find, order, purchase, ship, shipping, available, unavailable, ebook, epub, pdf, book, product, price, deal, discount, out of stock, import
  - Info/Research: where, how, hours, open, closed, address, directions, reach, contact, phone number, recommend, recommendation, review, kosher, nearby
  - Overlap between lists (e.g. "find" in Purchase, "phone" in Tech support) is intentional — resolved by the disambiguation UI, not by over-engineering the lists.
- Tech support confirmed as its own category (previously undiscussed until now — a real gap in earlier planning, since several of Gavi's real examples are tech support).
### Triage approach — DECIDED (2026-08-08, revised): simple form, no LLM
- LLM ruled OUT for v1. Reasoning: privacy concerns, and a deliberate principle — don't make a non-critical feature depend on a third-party interface to function.
- v1 intake: a free-text box + preset checkboxes (request type, urgency, possibly other fields TBD). Fully deterministic, runs entirely on Gavi's own server, no external calls, no data leaves the infrastructure.
- History: earlier direction was LLM-first conversational triage (rejected decision-tree for forcing categories); then local-LLM was considered (rejected — still an always-on third-party-interface-shaped dependency, plus deployment/always-on problems); landed on simple form as the version that actually satisfies both "no forced rigid categories" (checkboxes aren't rigid branching, no adaptive dead-ends) and "no non-critical third-party dependency."
- Open: exact checkbox taxonomy (request type options, urgency scale) — TBD.
- Claude API may still be used elsewhere in the app (this decision is about triage specifically, not a ban on all AI usage in the project).
### PWA (Progressive Web App)
- PWA = a website that Chrome/Android can "install": home-screen icon, own window, optional offline support.
- Minimum requirements: HTTPS + a manifest.json (name, icons, colors) + a service worker (can be near-empty). With Vite there's a plugin (vite-plugin-pwa) that generates most of it.
- Effort: hours, not days. Safe to consider MVP-cheap.
- **iOS:** PWAs DO work — installed via Safari's Share → "Add to Home Screen" (not Chrome; Chrome on iOS can't install PWAs). Once installed they run standalone (own icon, no browser chrome). Limitations vs Android: install flow is manual and non-obvious (no install prompt), and some capabilities are more restricted. Push notifications work on iOS only for installed PWAs (iOS 16.4+). Good enough for a friends-audience app; may want a small "how to install on iPhone" instruction page.
### DB choice notes
- Using BOTH DBs adds real complexity (two connections, two data layers, two things to break) for little benefit at this scale — not recommended.
- "Users switch between open and old requests" is not a DB-choice factor — it's a status field + a query filter in either DB.
- The actual decision drivers: relational shape of the data (users ↔ tickets ↔ messages ↔ credits is naturally relational → Postgres/Prisma), vs. flexible message/chat documents (→ Mongo). Both workable; decision deferred to planning phase.
- Split-by-domain (Mongo for chat, SQL for the rest) discussed: technically valid pattern at scale, but at this scale it's a waste — chat messages are perfectly served by a relational `messages` table (id, ticket_id, sender, body, image_url, timestamp), and the split costs two connections, two ORMs/clients, two backup stories, and cross-DB queries (e.g., "ticket + its messages") become application-level joins. Recommendation: one DB. (Counter-consideration: using both could demo more skills for the course — depends on requirements.)
### Deployment notes
- Gavi's existing pattern: Vercel (frontend) + Render free tier (backend). Pattern is fine and familiar.
- Render free tier caveat: services spin down after ~15 min idle → first request takes ~30–60s cold start. Annoying for a "concierge" app where friends expect responsiveness; also embarrassing in a live course demo.
- Options: cron-style ping to keep Render warm (common workaround), Railway/Fly.io free alternatives, or paid-tier Render (~$7/mo) for the demo month. Also check whether the course mandates a deployment target.
- DB hosting: MongoDB Atlas or Neon (Postgres) free tiers — accounts already exist.
### Fullstack build feasibility & ownership split (2026-08-16)
- Separate "Feasibility of manual project implementation" chat established: against the full PRD, Gavi hand-writing "most" of the code in the available time (15 days × ~3.5-4hrs, later revised upward) is not realistic — the scope needs to shrink, not just his involvement.
- Real hour-costed backlog built (not day-slotted — day-slotting produced artificially sparse/full days once actual per-task hours were estimated). Kanban/sprint-friendly: a flat task list with hour estimates, freely reorderable except along the dependency spine (DB schema → Express skeleton → Auth → Request model, everything else branches off that).
- Ownership split (decision #31): **You** write logic/structure — backend routes, DB, request/messaging/lifecycle/admin/credits logic, frontend component structure and wiring. **Agentic, fully explained**: design system + styling passes (based on Gavi's existing inspo board, not Tailwind — Gavi doesn't know Tailwind), OAuth wiring, E2E encryption, PWA/service worker config, CI/CD, deploy. **Collaborative**: test scaffolds, built per-feature as each one lands (Gavi wants to learn testing and CI/CD, not just receive them finished).
- Frontend design/component styling was an initially missed scope gap — full PRD requires designing and building every page/component, not just wiring logic to endpoints. Resolved via a one-time agentic "design system foundation" (base component styles, typography, tokens, derived from Gavi's inspo board) after which each UI task splits into structure (Gavi writes) + styling (agentic applies design system).
- React frontend setup and PWA manifest/service worker were also initially missed as their own line items (nothing currently exists — no frontend, no backend) — now explicit tasks.
- Fallback rule (decision #31, corrected by #41): if Gavi is falling behind on a task that's currently his, moving it to agentic is an option he can invoke — not an automatic trigger. This is the live adjustment mechanism instead of trying to perfectly predict the schedule up front.
- Agentic-built pieces Gavi wants to personally rebuild/study post-deadline (design system passes, OAuth wiring, E2E core, PWA/service worker config, deploy) are tracked separately in `gavi411-post-deadline-learning-backlog.md` — not a PRD item, not a decision, just a running reference for future-you.
- Full real-hour backlog (~82.5hrs total: ~47hrs Gavi build + ~9.5hrs collaborative + ~26hrs agentic) exceeds a naive "15 days × 4hrs" (60hrs) estimate by ~22hrs — explicitly surfaced as a real gap, not glossed over. Decision: proceed with the plan as scoped and apply the fallback rule live rather than pre-cutting further.
### Multi-agent workflow reconciliation (2026-08-16)
- The ownership split above was reconciled against an earlier, separately-planned multi-agent setup (Aegis Method + Repowise + Impeccable, from the "Multi-agent Claude project with frameworks" chat) — confirmed these are not in tension.
- "Agentic" work in the ownership split is actually **multiple subagents** committing code, with Repowise tracking agent provenance from git history and Aegis-style discipline (falsifiers, evidence bar, adversarial review, closure-against-reality) governing how that work gets closed out — not just "Claude does it and explains it."
- Commit-triggered agents (react to Gavi's commits): test scaffolding, CI/CD, design/styling passes (agent picks up a commit of plain structure and applies the design system to it).
- NOT commit-triggered, run in parallel/upfront instead: OAuth wiring (scaffolding Gavi builds against, not a reaction to his code), E2E encryption core (keypairs/ECDH/AES-GCM is a parallel subsystem that later integrates with messaging once that's committed, not triggered by any single commit).
- Two of the three deliverables originally requested from this reconciliation (Jira issue template, commit convention) are now built — see decisions #34–35 and the standalone files. The third (PRODUCT.md) is not a planning-chat deliverable at all — see decision #36.
### Design system setup — how the inspo board actually feeds Impeccable (2026-08-16)
- Impeccable does not ingest an external site or "point at" a reference. It only reads two files it expects in the project root: `PRODUCT.md` (strategy, written interactively via `/impeccable init`) and `DESIGN.md` (visual system — colors, type, components, radii), the latter generated by `/impeccable document`, which scans the **actual Gavi411 codebase** — existing tokens, components, rendered output — not any external reference.
- The inspo board (a separate site Claude Code built earlier to hold reference screenshots) does **not** transmit design direction through its own code. Its CSS/markup is just the gallery shell built to display references — it was never styled to embody the direction itself, so pointing Claude Code at that site's own styling is a dead end (corrects an earlier wrong suggestion in this chat).
- What actually carries the direction: the **screenshot image files themselves**, wherever they live on disk inside that project (e.g. an assets/images folder). Claude Code can view these directly as images, same as a fresh upload.
- Correct sequence: (1) point Claude Code at the inspo board's actual image files, or re-upload the specific screenshots directly into the session; (2) Claude Code builds Gavi411's first real components informed by those images; (3) once real styled code exists, run `/impeccable document` to scan *that* into `DESIGN.md`. Impeccable only ever sees code that already exists in the project — the inspo board influences the code first, Impeccable reads the code second, never the board directly.
- Practical implication for the design-system-foundation setup task: don't run `/impeccable document` first. Show the screenshots, build a couple of components/pages against them, then document.
### "Wrap it up" codeword — from prose to enforced checklist (2026-08-19)
- Originally a paragraph in `CLAUDE.md`: scope check, falsifier, Aegis fields, commit, done. No enforced order, no forcing function.
- Real failure observed: G411-11's Jira ticket sat at Open across multiple sessions even after "wrap it up" was said twice. The habitual, doc-adjacent steps (commit, HANDOFF.md) ran every time; the step requiring a separate tool call to a different system (the Jira status transition) silently didn't — and nothing surfaced that gap until it was noticed independently, days later.
- Root cause: a multi-system side effect (git + docs + Jira) driven by one prose instruction with no self-check. Aegis's Falsifier/Evidence fields gate *content* quality (is the claim true, is it proven) — they don't gate *execution* (did every step of closing actually happen). Two different jobs; conflating them was the mistake.
- Fix: `CLAUDE.md`'s "Wrap it up" section rewritten as an 8-step checklist (scope → falsifier → Aegis fields → evidence bar → Jira transition → commit → HANDOFF.md → report back), where the final step is a mandatory itemized ✓/✗ report, not a prose summary — so a skipped step is visible in the same turn instead of discovered later.
- Scope of the fix: this makes skips *loud*, not *impossible*. There's still no hook, CI check, or enforcement layer — it's still a human saying the codeword and a model correctly executing every step. True enforcement (e.g. a git pre-push hook or CI gate keyed to Jira status) was discussed and explicitly deferred — out of scope for what CLAUDE.md prose can do, and not worth building for a solo 1-month course project right now.

### Aegis "Landed" state — spec read directly, workflow corrected (2026-08-19)
- `Aegis-spec.md` was cited throughout the project as the primary source for the Aegis Method but had never actually been opened/read start-to-end before this session — everything relied on the derived template and secondhand summary instead.
- Reading it directly (§4.1) resolved a real confusion: Jira's workflow is **five** states, not four — Open → Implementing → Reviewing → **Landed** → Reconciled. Landed = merged/live (code shipped, real running state) but acceptance criteria not yet re-validated against it. Reconciled = acceptance formally re-checked against landed state (§5.5, "Closure Against Reality") — the true terminal "Done."
- The live Jira workflow had a `Landed` column with 0 issues and no transitions routing into it — not stray board config (my first wrong guess), and not something to delete (my second wrong instinct, before actually reading the spec). It was the workflow being incomplete relative to the spec it's supposed to implement.
- Fixed: named transitions added and published (Open→Implementing, Implementing→Reviewing, Reviewing→Landed, Landed→Reconciled) plus a global "Any→" reopen transition on all five statuses including Landed. Confirmed live via the Jira transitions API.
- `CLAUDE.md`'s Jira section and the wrap-it-up checklist's Jira-transition step both updated to name the real five states and the two separate closing transitions (→Landed, then →Landed→Reconciled) instead of collapsing them into one.
### New `agent-frontend` role (2026-08-19)
- G411-15 (PWA manifest + service worker, `[Agentic]`) didn't fit any of the 5 existing agent roles: not styling (`agent-design`), not deploy/CI (`agent-cicd`), not server-side (`agent-backend`). The prior "no agent-frontend role" ruling in `gavi411-commit-convention.md` was specifically about frontend *product* code (`[You]`) — it doesn't rule out a role for client-side *infra*, which is a different thing that was simply undecided until now.
- Added `agent-frontend` as a 6th role/worktree (`../Gavi411-agent-frontend/`, `agent-frontend@gavi411.local`), deliberately scoped narrow: build config, service worker, install/manifest plumbing — never product UI. Frontend component structure/wiring/pages stay `[You]`; the design/styling pass on top stays `agent-design`'s job. Explicitly reusable for future agentic client-side infra work, not a one-off PWA-only role — but the product-UI boundary must hold or it collapses back into the thing the original "no agent-frontend" ruling was guarding against.

### Decision #91 — a docs-only-titled commit landed a full feature on `main`, unreviewed (2026-09-01)
- Real incident: commit `302f463` (message: "commit-convention.md/brain.md: log decision #88") actually committed the entire pre-Sibling-review, pre-Matan-fix version of PR #35's device-linking feature (App.jsx, deviceLinking.js, crypto.js, devices.js, a schema migration, ~840 lines) directly onto `main`, alongside the two doc files the message named. Confirmed via `git show 302f463 --stat` and `git log -1 --format="%P"`: an ordinary single commit, direct parent on `main`'s own linear history, no merge/rebase involved.
- Root cause: a broad `git add` (`-A`/`.`/`-a`) run for what was intended as a small "log a decision" commit, in a working tree that also had unrelated feature files sitting modified/untracked at the time. The commit message was written to match *intent*, never checked against the actual `git status`/`git diff --cached` before running `git commit`.
- Impact: this bypassed the no-direct-commits-to-main rule, `gavi411-commit-convention.md`'s per-role branch scheme, and both Sibling review rounds — the version that landed on `main` was missing `requireAdmin` middleware and the ownership check that the real PR #35 review rounds added. Surfaced only because PR #35 later showed merge conflicts against `main` on the same files; resolved by merging `main` into the PR branch and taking PR #35's reviewed version on every conflicting file (see PR #35 for the resolution commit).
- Fix going forward: **before running `git commit`, always check `git status`/`git diff --cached --stat` and confirm the file list matches what the message claims** — especially for small, low-ceremony commits (a decision-log entry, a doc fix) since those are exactly the cases most likely to skip a deliberate review step. Never trust a commit message's stated scope as evidence of its actual diff. Logged in `CLAUDE.md`'s Required workflow section too.

### Decision #92 — G411-27 (encryption-at-rest fallback) Reconciled without code, condition never triggered (2026-09-01)
- Per decision #28: encryption-at-rest (Node `crypto`, AES-256-GCM, key in a server env var) was always the "guaranteed v1 fallback if time runs short" for messaging security — to be built *instead of* full E2E only if G411-28 didn't land in time. Never a defense-in-depth layer meant to sit alongside E2E. Decision #81 independently drew the same line (server-side EaR vs. client-side E2E) while scoping G411-82.
- G411-28 (target E2E) did land in time — device-linking merged (PR #35), search index built/reviewed/merge-ready (PR #36, blocked only on a human review approval, not on more work). The live message thread is wired to E2E (G411-82), not to any at-rest scheme.
- Since the triggering condition ("time runs out") never occurred, G411-27 has no remaining use — building it now would mean standing up a second, strictly weaker (full-server-compromise-exposes-everything) encryption path with zero consumers.
- Moved G411-27 directly Open → Reconciled (skipping Landed — nothing landed, there's no code) via the global "Any→" transition, with the disposition and reasoning written into the ticket's own description for anyone reading it standalone later. No commit, no branch, no PR — a documentation-only resolution.
- Precedent: a ticket can be legitimately Reconciled with zero code when its own precondition is confirmed to have not occurred — that's a real terminal state for a contingency ticket, not a workflow misuse. `gavi411-prd.md` §5 updated in the same pass so the PRD itself doesn't still describe EaR as live/pending fallback.
- **Follow-up correction, same session**: initially filled the Aegis story into the ticket description only, then (when asked about a Resolution-field gap) nearly repeated the mistake by writing Claim/Falsifier/Evidence into a *comment* instead of the real Jira custom fields — imitating G411-82's own precedent, which itself only carries Aegis content in comments, not fields. Caught before writing it: the real custom fields (`customfield_10073` Evidence required to close, `_10074` Role, `_10075` Evidence bar met, `_10076` Falsifier, `_10077` Owner/Authorship, `_10078` Reviewer type — confirmed via `getJiraIssueTypeMetaWithFields` with `requiredFieldsOnly:false`) were sitting empty on G411-27 the whole time; filled them properly instead. **Standing gap to watch**: other Reconciled tickets built earlier in the project likely have the same empty-Aegis-fields pattern (visible directly in Jira's issue list columns, e.g. Evidence required to close = None) since the working habit had been "write Aegis content into comments/description," not the dedicated fields — worth a real audit pass across G411's Reconciled backlog at some point, not urgent but real. Also confirmed live: this project's Jira Resolution field only has one value ("Done") configured — no "Won't Do"/"Cancelled" distinct from a real ship. Adding one is a Jira Settings UI action outside what these MCP tools can do (resolution scheme management isn't exposed); Gavi would need to add it manually if the distinction is wanted later.

### Decision #93 — Web Push is the primary notification channel for admin too, not friends-only (2026-09-01)
- Real ambiguity found live: PRD §4.3/CLAUDE.md's summary line read "Web Push for friends, Telegram for Gavi" — built during G411-29 (Web Push infra), a Sibling review flagged that `notifyAdminOfDeviceRequest` pushing to admin via Web Push contradicted that line.
- Gavi's call: that phrasing was never meant as an exclusive split — it described which channel was expected to matter most for whom, not a hard boundary forbidding admin from also getting Web Push. Real intent (already consistent with PRD §6.1's own priority table, decision #45: Telegram is "Should," explicitly deprioritized behind Web Push, "acceptable to wait if Web Push alone covers notification needs"): **Web Push is the primary channel for everyone, friends and admin alike; Telegram is a secondary/supplementary channel for Gavi specifically**, not a replacement.
- `CLAUDE.md` and `gavi411-prd.md` §4.3 both corrected to state this precisely instead of the ambiguous shorthand. `devices.js`'s admin-via-Web-Push wiring (G411-29) stands as correct, not a bug — no code change needed, this was a doc-vs-intent gap, not an implementation gap.

### Decision #94 — `/code-review --comment` posted broken PR comments (local scratch-file paths instead of content), caught and repaired live (2026-09-01)
- Real incident during G411-29/PR #37's Sibling review: 7 of the 10 inline PR comments the `--comment` flag posted were literally the string `@/tmp/claude-1000/.../scratchpad/pr37comments/cN.md` — a local file path — instead of the actual finding text. Confirmed via `gh api repos/.../pulls/37/comments` reading the raw comment bodies back.
- Root cause not fully diagnosed (outside this project's control — it's the `code-review` skill's own posting path, not something in this repo), but the scratch files themselves still existed on disk with the real content, so recovery was possible: read each `cN.md` file directly, then `gh api -X PATCH repos/.../pulls/comments/<id>` to overwrite each broken body with the real content. All 7 verified fixed by reading them back afterward.
- **Standing practice going forward**: after any `/code-review ... --comment` run, spot-check at least one posted comment's actual body via `gh api` (or the PR page) before trusting the "N comments posted" summary — the tool's own success report doesn't catch this class of failure, since the post itself succeeds, it just posts the wrong content.

### Decision #95 — G411-83/84 explicitly queue-jumped ahead of G411-79, a one-time override not a new default (2026-09-01)
- Gavi's direct instruction after G411-28/29 both landed: work G411-83, then G411-84, then G411-79 — in that order, ahead of strict epic/key order (which would otherwise put G411-79 first among the remaining Messaging-epic Open children).
- This is a deliberate, explicit exception for this specific stretch — not a reversal of the standing "epic order, not severity" default. Both G411-83 and G411-84 build directly on infrastructure that just landed (G411-28's `Device` model, G411-29's Web Push), so sequencing them next while that context is warm is the actual reason, not a general re-ranking of the backlog by importance.
- After G411-83/84 are done, queue order reverts to normal (G411-79 next, then wherever the backlog naturally continues) — a future session should not read this as "84 and 83-adjacent tickets always jump the queue."

### Decision #96 — real production bug found live: image-send has never worked on the deployed backend (CLOUDINARY_URL never set on Render), plus VAPID never set either (2026-09-01)
- Gavi reported a real failure: sending a message with an image attached failed on his phone; text-only sends worked. Confirmed live via Render's own service logs (not guessed) — `Error: Must supply api_key` thrown from Cloudinary's SDK inside `uploadImage()` (`server/lib/cloudinary.js`). Root cause: `CLOUDINARY_URL` was never declared in `render.yaml`, so Render's dashboard never prompted Gavi to set it and it was never actually present on the live service's environment — a gap that pre-dates this session (Cloudinary/G411-26), not a regression from G411-28/29's changes.
- Same logs also showed the VAPID misconfiguration error added this session (`server/lib/webPush.js`'s `ensureVapidConfigured()`, itself a Sibling-review-driven fix) firing for real — confirming the three `VAPID_*` vars are also missing from Render's actual environment, same class of gap, introduced today and caught same-day instead of going unnoticed.
- Fixed: `render.yaml` now declares `CLOUDINARY_URL` and all three `VAPID_*` keys (all `sync: false`, matching the existing pattern for secrets). This does NOT retroactively populate the live service — Gavi still needs to manually enter the real values in Render's dashboard before image sends or push notifications will work in production. Local dev (`.env`) was never affected — this was purely a deployed-environment gap.
- **Standing gap, not yet closed**: no process currently catches `render.yaml`/`.env.example` drift automatically — this is now the second time a required env var was added to `.env.example` without a corresponding `render.yaml` entry (Cloudinary originally, VAPID this session). Worth a lightweight check (e.g. comparing the two files' key lists) before/at deploy-affecting tickets going forward, though no such check exists yet.
- Also surfaced live during the same test: a second, separate bug — a friend's device-link approval doesn't take effect in an already-open session, and (per Gavi's direct correction) **not even after a manual page reload** on the device that requested linking. This rules out the initial one-shot-`useEffect` theory as the *complete* explanation (a reload should re-trigger `loadLinkedConversationKeys()`) — root cause not yet found, investigation paused to prioritize the image-send bug first. Still open, needs its own dedicated trace once image-send is confirmed fixed.

### Decision #97 — G411-85 filed: 4 real, confirmed regressions in E2E messaging/device-linking, found live testing after G411-28/29 landed (2026-09-01)
- Full live-testing session (admin phone + a real test-account PC) surfaced four distinct, code-confirmed findings, all now in G411-85 (parented under G411-3):
  - **(A)** A device-linked browser can read messages after a refresh but the send path doesn't recognize it as usable — `sendMessage()`'s precondition check disagrees with `getConversationKey()`'s successful read-path derivation.
  - **(B)** "Generate my encryption key" is destructive and unrecoverable once a browser has ever called `requestDeviceLink()` — `getConversationKey()` permanently short-circuits to linked-device-only mode via a `deviceId != null` check that never falls back to direct ECDH again, and nothing anywhere clears the saved `deviceId`. Confirmed via a real repro: a test user's working, readable message became unreadable the moment they clicked "generate" after already being linked.
  - **(C)** Images are never encrypted, contradicting decision #47's explicit PRD line ("Images get the same treatment"). This was a scope cut made silently during G411-82's build, documented only in a code comment, never brought to Gavi for sign-off.
  - **(D)** The generate/request-key recovery flow is only reachable as a side effect of a failed send — not a standing, visible state when a user opens a thread they can't yet decrypt.
- **G411-82 (Reconciled) now has a real status/reality mismatch** — its Falsifier text implies full encryption coverage that finding (C) shows isn't true. Flagged via a comment on G411-82 itself, not silently reopened — that transition is Gavi's call.
- Real damage from this session's testing: one test message (id 22, pre-dates any device link) is permanently unreadable by design; a second (id 24) was readable, then made unreadable by finding (B) — both on test data, not real friend data, but the same sequence against a real account would be equally destructive with no UI recovery path today.
- Standing lesson: a build-time scope cut that deviates from an already-written PRD decision needs to be surfaced for explicit sign-off at build time (AskUserQuestion or equivalent), not just documented in a code comment and left for a future live-test to discover by accident.

### Decision #98 — E2E architecture reframed to escrow-only (no device-linking mechanism); all accounts wiped and re-onboarded; encryption paused, rest of the product finishes first (2026-09-02)
Follow-on to decision #97, worked through live with Gavi over a long back-and-forth after G411-85's findings, not a quick call. **Full current status, target architecture, and every open question live in `gavi411-e2e-encryption-plan.md`** — that doc gets updated as the plan evolves; this entry stays as the historical record of the decision itself.

**1. Root framing correction (Gavi's, not derived by Claude alone)**: the user should never be asked to make any key-related decision — no "generate," no "request access," nothing resembling a choice. WhatsApp is the reference: users are never asked to do anything like this. Presenting device setup as a user-facing choice between two buttons (as G411-82/28 shipped it) is itself the root defect finding (D) sits on top of — not just a UX polish gap.

**2. Target architecture, reasoned from first principles, not from defending what already existed**: a single mechanism — **escrow, and only escrow** — replaces the entire `Device`/`ConversationDeviceKey`/request-approve-rewrap system. Every account, including admin, gets exactly one escrow backup created automatically at first real key generation (invisible to the user). Any device that needs to become "live" for an account — first device, second device, a replacement after loss — does so by supplying that account's escrow passphrase and decrypting the backup locally. There is no second party who "approves" a device; admin does not vouch for a friend's new device the way today's device-linking has admin do. Admin having multiple simultaneously-live devices is not a special case requiring separate machinery — it's just admin unlocking the same escrowed key on more than one device, identical in kind to a friend doing the same. This eliminates the `deviceId != null` short-circuit in `getConversationKey()` that caused finding (B), and collapses read/send path disagreement (finding A) since there's only ever one key-acquisition mechanism to keep in sync.
- Real tradeoff, named explicitly rather than hidden: a friend without their original passphrase still needs a human (Gavi) to hand it back to them (via the existing CSV/1Password pattern) to add a new device or recover from loss — this is not literally zero human involvement, it trades "approve a device request" for "look up and resend a passphrase," roughly comparable operational cost, but as ONE mechanism instead of two.
- Escrow's own role is unchanged and reaffirmed, not new: it is specifically the answer to "the one live device is lost before any other device was ever set up" — no other mechanism can cover that case, since it requires a trusted party to already exist, and if the account's only device is gone, none does.
- Buildable within the existing free-tier stack — this is fewer moving parts than what's there today, not new infrastructure.
- Not yet decided/built: exactly how a second device acquires the passphrase in practice for the multi-device (admin) case day-to-day — worth its own design pass, not assumed solved by this decision alone.

**3. All accounts wiped and re-onboarded through whatever the actual final flow becomes** — admin included. Every current account predates a coherent key story (admin's own key has zero escrow backup at all — the G411-83/decision #86/#87 gap that was never actually closed; test accounts are mid-experiment from tonight's testing; one account has no key at all). There is no real user data to protect pre-launch, so wiping and re-onboarding is strictly cheaper and lower-risk than migrating broken state forward. Not yet executed — planning first, per Gavi's explicit instruction this session.

**4. Encryption is paused; the rest of the product finishes first.** Message content reverts to plaintext-in-DB (as it was before G411-82 wired encryption in) while Request Lifecycle, Admin Cockpit, Credits, and Notifications epics — the actual bulk of the course deliverable — get built and finished. E2E encryption becomes explicitly optional/stretch again, matching the PRD's own original framing ("Target design (stretch goal)"), revisited only if time remains after the rest of the product is done. Gavi's own clarification, important to log precisely: **the crypto primitives themselves did land successfully and are not in question** — AES-GCM encrypt/decrypt and ECDH derive work correctly (per the Stage 1 Aegis Falsifier and G411-82's own live-tested round-trip). What's unreliable is the surrounding system: key lifecycle, device state propagation, and discoverability — the exact things G411-85 found broken. This is not a correctness failure in the cryptography, it's an unfinished/overcomplicated system built around correct cryptography.

**5. Real tension with a same-day prior call, corrected here rather than left contradicting itself**: decision #92 (this same session, earlier) Reconciled G411-27 (the encryption-at-rest fallback) specifically because "G411-28's full E2E design... did make it — the triggering condition for this fallback never occurred." Given #98's pause on encryption, that condition may be occurring after all, just discovered hours later than #92's Reconciled call assumed. G411-27 is NOT being reopened by this decision alone — that's a separate call for whenever the final encryption-or-not outcome is actually known (possibly at the end of the course timeline, per point 4) — but the contradiction is real and should be resolved explicitly then, not silently.

**Status**: this is a planning decision, not yet executed. Per Gavi's explicit instruction, the next work is finishing the plan (see HANDOFF.md for the concrete open questions) before any code changes — do not start wiping data or touching the messaging encryption path until that planning is actually done and confirmed.

### Decision #99 — Aegis custom-field IDs now written durably into the template doc, after the same gap recurred a second time (2026-09-02)
- Decision #92 (same day, earlier) already caught this once on G411-27: Claim/Falsifier/Evidence/Scope/Role/Owner-Authorship/Reviewer-type are real Jira custom fields on the Task screen (`customfield_10073`-`10079`), not description prose — and got fixed on that one ticket. It recurred anyway on G411-86 (this session): wrote everything into the description again, then concluded from a `customfield_*: null` dump that the fields didn't exist project-wide (wrong inference — null on one issue proves empty, not absent), before Gavi pointed at G411-27 as a live counterexample and it was corrected again.
- Root cause of the recurrence: decision #92's fix lived only in that one ticket's Jira history and a `getJiraIssueTypeMetaWithFields` call made live in that session — never written into `gavi411-jira-aegis-template.md` itself, so the next session (or subagent) had nothing durable to check against and had to rediscover the field IDs from scratch.
- **Fixed properly this time**: `gavi411-jira-aegis-template.md` now carries the real field-ID table directly (`customfield_10073` Evidence required to close, `_10074` Role, `_10075` Evidence bar met — 255-char plain-text cap, `_10076` Falsifier, `_10077` Owner/Authorship, `_10078` Reviewer type, `_10079` Scope), the single-select `{"id": "<option id>"}` write format, and an explicit warning against the exact wrong inference made this session (null-on-one-issue ≠ field doesn't exist — check `expand:"names"` or a populated example like G411-27 instead). PR #39, merged.
- Standing lesson, generalized beyond just this field table: a fix discovered live in one session, that isn't written into a doc a future session actually reads before acting, doesn't durably exist — it's rediscovered at the same cost every time it recurs. This is the same shape as the "log a decision when it's made, not just at wrap-up" rule already in CLAUDE.md, but specifically about *reference* facts (field IDs, tool quirks, API shapes) rather than *process* decisions — worth remembering the distinction: a process decision goes in brain.md, a reference fact needs to go in the actual doc a session will consult when doing the task, not just be logged as having happened.

### Decision #100 — Cancel/self-solved refund eligibility rule: gated on zero ADMIN-role messages, not general "untouched" status (2026-09-02)
- PRD §4.4 only says "cancelled (refund, especially if untouched)" — vague on both whether self-solved also refunds and what "untouched" precisely means. Resolved live with Gavi while scoping G411-31: **both** cancelled and self-solved refund 1 credit, gated specifically on **no ADMIN-role user having sent a Message on the request yet** — not on request status (e.g. still IN_QUEUE). Gavi's own words: "refund both if I didn't touch it (one message from me is fine, I might be asking a clarifying question)." So a request sitting in RECEIVED with zero admin messages is still refund-eligible; one admin message at any point (even a short clarifying question) permanently disqualifies that request from a refund on a later cancel/self-solve.
- Implemented as `hasAdminMessaged(db, requestId)` in `server/lib/requestAccess.js` — queries for any `Message` row on the request where the sender's `role` is `ADMIN`. Called from inside the same `$transaction` as the refund + status write (not before it) — an earlier draft checked this outside the transaction and a Sibling review (PR #42) correctly flagged it as a real double-refund race (two concurrent cancels could both observe "no admin message" before either transaction committed).
- G411-31/45/48 were built as one combined pass (Gavi's explicit call) since each ticket's own Jira description deferred scope to the others and none was independently buildable as scoped — see PR #42. G411-45's slice here is *only* the schema/initial-grant/shared-helper piece; the credit balance UI display surface stays its own separate, still-Open ticket.
- Real gap fixed in the same pass, unrelated to the refund rule itself but touching the same code: new users were getting `creditBalance: 0` at signup with no tiered initial grant at all (PRD §9: Limited 2/Regular 5/Close 7) — a brand-new friend couldn't submit even a first request until manually topped up. Fixed, but **tiering is still not actually reachable in the running app** — `groupTag` is never set anywhere before/at signup (defaults to the schema's own `REGULAR`), so every new user gets exactly 5 credits regardless of intended tier today. LIMITED/CLOSE only activate once a separate ticket adds real groupTag collection at signup.

### Decision #101 — Urgency downgrade: admin gets free any-direction control, friend keeps the PRD's narrower one-way rule (2026-09-02)
- PRD §4.4 says only "friend may downgrade to 'no longer urgent'" — says nothing about admin's own ability to set urgency. While scoping G411-32, Gavi pushed back on inheriting that restriction unquestioned ("is there a reason this scope exists? I didn't define something like this") — right call, since the line was an old planning-doc assumption never actually re-examined for whether it should also bind admin.
- Resolved live: **admin (ADMIN role) may set a request's `urgency` to any value, in any direction** — no restriction, matching the free control Gavi already has over `status` (`canAccessRequest`'s admin bypass). **A non-admin (friend) keeps the PRD's original, narrower rule**: only `HIGH -> NORMAL` is a legal write; every other direction/value (e.g. `NORMAL -> HIGH`, `LOW -> HIGH`, `HIGH -> LOW`) is rejected for a non-admin caller.
- This is a genuine, deliberate divergence from a literal PRD reading (which never distinguished admin from friend for this action) — worth remembering as the reasoning next time a similar "PRD says X for friend, doesn't say anything about admin" ambiguity comes up: default to admin having the fuller access already established elsewhere in the app (status transitions, request visibility) rather than assuming the friend-side restriction was meant to bind admin too.

### Decision #102 — Close is friend-only (not admin), distinct from auto-close; reopen-on-message target status depends on who messages (2026-09-02)
- **Close actor gate (G411-33)**: PRD §4.4's "Gavi asks 'did this resolve it?' → friend confirms → closed" reads as a friend action, but the earlier admin-gets-free-access pattern (decision #101) made it worth checking rather than assuming symmetry. Gavi's explicit call: manual close via this flow is **friend-only** — admin cannot close a request this way, deliberately diverging from the "admin gets fuller access" default set in decision #101. Reasoning, Gavi's own words: "I want to have friend confirmation that it's ready to close and not just close on my own." This is distinct from **auto-close** (G411-35, still to build) — a 14-day timeout closes a request without friend confirmation by design; the two paths intentionally have different actor rules for the same terminal state.
- **Reopen target status (G411-34)**: PRD says reopening "doesn't re-triage" but gives no explicit target status for what a reopened CLOSED request becomes. Worked through live: **IN_QUEUE** is correct for a friend-triggered reopen, not RECEIVED as first suggested — the distinction is that IN_QUEUE means "Gavi hasn't seen *this* activity," not "this is a brand-new, untriaged request." Gavi's own words: "if they reopen it I haven't seen it, it goes back to the queue. It's not a brand new ticket but it's not 'seen' in the sense that I know it's reopened." "Doesn't re-triage" means continuations skip the intake/type-matching flow, not that the status enum must avoid IN_QUEUE. **An admin-triggered reopen (Gavi messages on a CLOSED request) goes to WAITING_ON_USER** — symmetric with the existing WORKING_ON_IT↔WAITING_ON_USER pattern (whoever just acted, the other party is now expected to respond).
- Both built together as G411-33 (`server/routes/requests.js` PATCH `/:id`, actor gate on the CLOSED transition) and G411-34 (`POST /:id/messages`, status side-effect when messaging on a CLOSED request) — tracked as separate Jira children since they're independently falsifiable, even though they touch adjacent code in the same PR.
- Sibling review (PR #47) found and fixed a real TOCTOU race in the first draft of G411-34: `existing.status` was read via a plain `findUnique` BEFORE the transaction opened, so two near-simultaneous messages on the same CLOSED request could both see CLOSED from their own stale snapshot and both write a reopen — final status determined by commit order, not real message order. Fixed by only entering a transaction on the CLOSED path and re-checking status FRESH inside it before deciding to reopen.

### Decision #103 — Auto-close job design (14-day inactivity read off last message, not Request.updatedAt); self-merge on `main` actually requires `gh pr merge --admin`, not a bare merge (2026-09-02)
- **Auto-close job (G411-35/36)**: "inactivity" is measured from the request's LAST MESSAGE `createdAt` (or `Request.createdAt` if none) — never `Request.updatedAt`. Real trap caught before writing code: a plain `Message.create` never touches the `Request` row, so `updatedAt` only moves on a status/urgency PATCH — using it would make the inactivity clock go stale the moment a friend actually replies without also changing status. "Already warned, no reply since" is read off existing data (no new column): the warning is a real `Message`, authored by the admin account (no system-message concept exists in this schema, Gavi's call), with EXACT content match against a constant — not just "last message is from admin" (an early draft's false-positive: any ordinary admin reply would count as an implicit warning and let a request auto-close with none ever really sent; caught in Sibling review).
- **Scheduler**: in-process `setInterval` (6h cadence) in `server/server.js`, Gavi's explicit call — Render free-tier + no existing scheduler infra anywhere in the codebase yet, and the ticket itself scopes this as lightweight/not shared with the future Reminders feature (G411-59). Ceiling: misses checks during a Render cold-start sleep gap — accepted, marked with a `ponytail:` comment.
- **CLOSED write bypasses `TRANSITIONS`/`canCloseRequest` on purpose**: the auto-close job writes `Request.status = CLOSED` directly from `WAITING_ON_USER`, which `TRANSITIONS` deliberately does NOT list as a legal PATCH edge — adding it would let a FRIEND close their own request via a plain PATCH, skipping the RESOLVED_PENDING_CONFIRMATION confirm step decision #102 exists to require. This is a genuine system-actor exception: no vocabulary for a non-human actor exists in `canCloseRequest` today (it's shaped around `req.user`), so the job's own fresh in-transaction re-check (still `WAITING_ON_USER` right before writing) is its sole legitimacy gate instead of going through the HTTP-route guard. First draft shipped this with NO re-check at all (Sibling review finding — same TOCTOU class decision #102's PR just fixed, reintroduced via this separate call site) — fixed by wrapping the write in a transaction with a fresh status re-read immediately before it.
- **Refund-gate interaction (real cross-ticket bug, caught by a verify pass after the first fix round)**: the auto-close warning message, authored as admin, was indistinguishable from a genuine admin reply to `hasAdminMessaged()` (decision #100's refund-eligibility gate) — a friend who only ever got an automated nudge, never real admin engagement, would have been wrongly denied their G411-31 cancel/self-solved refund. Fixed by excluding the exact warning text from `hasAdminMessaged`'s query. Worth remembering generally: any future automated/system-authored message that reuses the admin account will need the same exclusion treatment against `hasAdminMessaged`.
- **Self-merge mechanics on `main`, real gap found live**: decision #63 says agentic work self-merges with no outside human approval required — but `main`'s actual GitHub branch protection requires 1 approving review, so a bare `gh pr merge --merge` is rejected ("base branch policy prohibits the merge"). Prior agentic PRs (#47 etc.) were in fact merged by Gavi's own account, not self-merged — the documented policy and the actual repo setting had silently diverged. Resolved live: use `gh pr merge --merge --admin` (uses repo-admin privileges to override the review requirement for that one merge) rather than changing the branch-protection setting itself — keeps the protection rule intact for any non-admin context while letting agentic work actually self-merge per decision #63's intent. Future agentic merges on this repo should use `--admin`, not a bare merge, until/unless Gavi deliberately changes the branch protection rule itself.

### Decision #104 — Real gap found live: G411-37 was never transitioned off Open at pickup, then wrongly reported as "Reviewing" mid-session without checking; a second, related miss the same evening on the Landed→Reconciled confirm itself (2026-09-02)
Working G411-37, an agent picked up the ticket and started coding without ever making the Open → Implementing Jira transition — the ticket sat at Open through 4 full Sibling review rounds while real work was happening. Mid-session, when asked why Jira statuses weren't updated, the agent stated the ticket was at "Reviewing" — a fact it had not actually re-checked, and which was wrong (still Open). Gavi caught the actual state himself ("I still see 37 as open, it was never even set to implementing"). Root cause: the session-start/pickup ritual's "this is also when the Claim/Falsifier/Evidence-required fields get written" step (decision #50) doesn't explicitly call out the Open→Implementing transition itself as a separate, mandatory action at pickup — it's easy to start coding and let the actual Jira state lag behind real progress, especially across a long multi-round review session where the ticket's Jira status never comes up again until someone asks. Compounded by CLAUDE.md's own "don't assert without checking" rule (decision-adjacent, see "Required workflow" #3) being violated in the same breath as this was noticed — stating "Reviewing" from memory instead of calling `getJiraIssue` first. Fixed live: transitioned Open → Implementing → Reviewing in the correct order once actually checked, Aegis fields written (late, but against real final state rather than left blank). Going forward: the Open→Implementing transition is not optional busywork deferred until "later" — it should fire in the same turn work actually starts, not retroactively once someone asks where the ticket stands.

**A second, related miss, same evening, after the fix above**: once PR #51 merged and worktrees were synced, a wrap-up report presented "Jira transition ✓ — G411-37 now Landed" as a checkmarked, done item, with the still-open Landed→Reconciled confirm never actually asked as a question anywhere in that message — the report moved straight to "Next on the spine: G411-44 or G411-38... your call which to pick up," which read as "everything is finished, pick the next task." Gavi had to separately ask "are we good to go, or is something left" before the reconcile confirm actually surfaced. When first asked to explain the gap, the agent additionally misstated that the original message HAD mentioned the open item as a buried aside — it had not; asked to quote the exact line, there wasn't one, compounding one real miss with a second, avoidable one (asserting what a past message said instead of rereading it). Both are logged and fixed in the standing memory `reconcile-confirm-plain-question.md` (session-level, not this file, since it's a per-turn behavioral habit): a wrap-up report must never show a Jira-transition checkmark for a ticket sitting at Landed without that same message also asking, in plain text, whether to reconcile it — Landed is not a "done" state in a wrap-up report, it's an open question. And: never assert what an earlier message said without rereading it first.

### Decision #105 — `agent-frontend` role is infra-only; product UI/page work (including agentic frontend tickets like G411-38) commits under `agent-backend`, from that role's dedicated worktree with its own git identity (2026-09-02)

Working G411-38, a session first branched as `agent-frontend/G411-38-...` and committed from the primary worktree under Gavi's own git identity (`gavriel.lazan@gmail.com`) — wrong on both counts, caught only when re-reading `gavi411-commit-convention.md` mid-wrap-up rather than before starting. The doc is explicit and was already correct — `agent-frontend` is deliberately scoped narrow (build config, service worker, install/manifest plumbing, added 2026-08-19 for G411-15's PWA work) and "never product UI"; frontend component structure/wiring/pages route through `agent-backend` (or `agent-design` for the styling pass specifically), and every agent role has its own persistent worktree (`../Gavi411-agent-<role>/`) with `git config --worktree` already set so the correct identity never needs manual switching. The mistake was skipping that check before branching, not a gap in the doc itself.

Recovery, logged for the mechanics rather than just the outcome: staged changes were sitting on `main` in the primary worktree by the time this was caught. Rather than force-discard blind, the diff was saved as a patch file first, verified to apply cleanly against a clean detached checkout of `main` in an isolated scratch worktree, *then* the primary worktree's copy was discarded and the patch re-applied and committed from `/home/gavi/Desktop/Gavi411-agent-backend` under its real `agent-backend@gavi411.local` identity. No work was lost, but it cost a full extra round trip. Going forward: confirm the correct agent role (and that its dedicated worktree, not the primary one, is where the branch/commit actually happens) against `gavi411-commit-convention.md` *before* creating a branch, not after staging — "product UI" is the trigger word that should route to `agent-backend`, not `agent-frontend`, every time.

**A second, real miss, same wrap-up, caught by Claude itself on review rather than by Gavi**: after G411-38 merged, the HANDOFF.md/brain.md updates for wrap-up step 7 were committed directly to `main` (`e408867`) from the primary worktree — no branch, no PR. `CLAUDE.md`'s own Branching rule ("no direct commits to `main`") makes no docs-only exception, and prior sessions' equivalent handoff commits (visible in `git log` as `you/g411-37-handoff-stop`, `you/g411-37-reconciled-and-wrapup-miss`) *did* go through a real branch+PR. Caught immediately (before the user had to point it out) by checking `git log` against the rule text rather than assuming the docs-update step is exempt. Gavi's call once flagged: leave `e408867` on `main` as-is (content is correct, low-risk, and rewriting already-pushed history for a docs commit is a worse trade than the violation itself) rather than revert+redo through a proper branch. Going forward: HANDOFF.md/brain.md commits are not a silent exception to the no-direct-commits rule — route them through a `you/`-prefixed branch and PR like every other change, every time, not just when the change is code.

### Decision #106 — Both G411-38 wrap-up mistakes (decisions #105's two entries) get mechanical guardrails, not just documentation, since both happened despite the correct instructions already existing in doc form (2026-09-02)

Gavi's framing, directly: "they are already both well documented, so what's the fix? more documentation isn't it." Correct — decision #105's two mistakes (wrong agent role/branch, then a direct commit to `main`) both happened *after* `gavi411-commit-convention.md` and `CLAUDE.md` already stated the correct rule in full. A third paragraph explaining it again doesn't change the failure mode, which was "read the doc at the wrong moment, or not at all, right before acting" — something has to make the wrong action either impossible or immediately loud, without depending on a re-read landing at the right second.

**Two guardrails added, deliberately different strength:**

1. **GitHub ruleset `require-pr-for-main`** (id `22145602`, created via `gh api repos/.../rulesets`), target `refs/heads/main`, one `pull_request` rule, **no bypass actor** — `current_user_can_bypass` confirmed `"never"` on creation. This is real teeth: a direct `git push origin main` is rejected server-side (`GH013: Repository rule violations`) regardless of local git config, identity, or intent — tested live against a throwaway commit before relying on it. Note this is separate from the *pre-existing* classic branch protection (`required_pull_request_reviews.required_approving_review_count: 1`, already active before this session, which is what made `gh pr merge --admin` necessary per decision #103) — the new ruleset's own PR-requirement is deliberately set to `required_approving_review_count: 0` so it doesn't stack a second approval gate on top of the existing one; it only closes the actual gap, which was that direct pushes to `main` weren't blocked at all (only PR-merge-without-review was).

2. **Repo-committed `.githooks/pre-push`** (not `.git/hooks/`, which isn't versioned) — checks the pushing git identity's role (`agent-backend@...`, `agent-frontend@...`, etc., per the role table in `gavi411-commit-convention.md`) against the branch prefix actually being pushed, and separately refuses `main` outright as a faster local-first check ahead of the network round-trip. Installed via `git config core.hooksPath .githooks` in all 7 worktrees this session. Explicitly documented as a **soft** tripwire — `git push --no-verify` bypasses it trivially, and it does not self-propagate to a future `git worktree add` or fresh clone without that same one-time `git config` line being run again (caught live during this session's own test: the hook file existed uncommitted in the primary worktree only, so testing it from `Gavi411-agent-frontend` silently no-op'd until the file was actually committed and pushed). The GitHub ruleset is the mechanism doing the real work; the hook is a convenience/speed layer on top, not a substitute.

**General principle, worth carrying forward past just these two mistakes**: when a mistake happens despite the correct rule already being documented and read, the fix is not "document it more clearly" or "add a third correction paragraph" — it's asking whether the wrong action can be made mechanically impossible (a server-side rule, a hook, a lint check, a type) rather than relying on the rule being re-read and correctly applied at the exact right moment every single time.

### Decision #107 — Two mechanism-level findings from the overnight G411-39/40/87/88 run: worktree-isolated agent sandboxes can reach the persistent role worktrees directly (just not through the shared primary checkout), and `gh pr merge` fails whenever another worktree holds `main` checked out (2026-09-02 night → 2026-09-03)

An unattended overnight session running G411-39, G411-40, G411-87, and G411-88 executed inside a worktree-isolated agent sandbox (a temporary path under `.claude/worktrees/`), not literally at a terminal in the primary `Gavi411` checkout. Two mechanism-level findings worth remembering for any future unattended/background dispatch in this repo:

1. **The sandbox's Bash tool refuses any command that routes through the shared primary checkout** (`cd /home/gavi/Desktop/Gavi411 && ...` — rejected outright, "a worktree-isolated agent's git operations must target its own worktree") **but a direct `cd` into one of the persistent per-role worktrees (e.g. `/home/gavi/Desktop/Gavi411-agent-backend`) works normally**, including git operations, pushes, and `gh` calls run from there. This meant the existing per-role worktree setup (identity, `core.hooksPath`) from prior sessions was reachable and usable without any new setup — worth checking directly (`cd` and inspect `git config`) rather than assuming a sandboxed dispatch can't reach the role worktrees at all.
2. **`gh pr merge` (and any command that does a local `git checkout`/`git worktree` operation on `main`) fails with `fatal: 'main' is already used by worktree at '/home/gavi/Desktop/Gavi411'`** whenever the primary worktree has `main` checked out — which it does essentially always, since that's its steady state. This is unrelated to the GitHub ruleset/branch-protection guardrails (decision #106) — it's a plain local git constraint (one worktree per branch, repo-wide). Worked around by merging via the REST API directly instead: `gh api repos/<owner>/<repo>/pulls/<n>/merge -X PUT -f merge_method=merge` — same merge-commit strategy (`merge`, matching decision #68's regular-merge-only rule), no local checkout of `main` involved at all, confirmed clean via `git log origin/main --oneline` after each merge rather than trusting the command's exit code alone. Worth using this API form by default for any future agent-worktree-dispatched merge, rather than rediscovering the failure each time.

### Decision #108 — A new Task's parent Epic must be non-Reconciled at filing time, no exceptions for "the related backend work happens to live there" (2026-09-03)

Filing G411-90/91 (real bugs found live during a manual walkthrough of the overnight batch), both were initially parented under G411-4 (Request Lifecycle) since that's where the *related* backend logic (`TRANSITIONS`, `REFUNDABLE_EXITS`, `canCloseRequest`) already lives — following the precedent of G411-33/G411-87, which sit there too. Gavi caught it immediately: "G411-90/91 can't be in epic 4 since that's a reconciled epic." The precedent didn't actually apply — G411-33/G411-87 were filed or picked up *while* G411-4 was still open; G411-4 has since gone all the way to Reconciled. Filing a brand-new Open task under an already-Reconciled epic silently breaks the parent-rollup invariant this project already has a standing memory for (`jira-parent-rollup.md` — "always update the parent Epic's status alongside a child Task's status transition"): the epic's own status now falsely claims "nothing left to do" while real unfinished work sits under it. Both moved to G411-5 (Admin Cockpit, still Implementing) instead — the real, current-session parent, not the epic the affected code historically shipped under.

**The actual rule, already stated once for G411-69 ("Foundation/G411-1 is Reconciled so isn't a valid parent per Gavi's standing rule that unparented tasks need a relevant, non-Reconciled parent") but not yet generalized**: when filing ANY new task, check the candidate parent Epic's live status before setting it — "this is where the related code lives" is not sufficient justification if that epic has already closed. Pick whichever currently-open epic is the next-best fit (usually wherever the finding actually surfaced this session), not the epic that historically owned the affected code.

## 7. Not Yet Discussed
 
- Data model, architecture, tech decisions (schema itself not yet drafted — first task on deck).
