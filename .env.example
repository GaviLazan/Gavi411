# Copy to .env and fill in real values. .env is gitignored.

# Postgres connection string (Neon) — used by prisma/schema.prisma
DATABASE_URL=

# Clerk (G411-13) — from https://dashboard.clerk.com > your app > API Keys
CLERK_SECRET_KEY=
CLERK_PUBLISHABLE_KEY=

PORT=3000
