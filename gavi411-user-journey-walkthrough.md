# Gavi411 — User Journey Walkthrough (find-and-report only)

Generated by walking all 9 journeys against: `gavi411-prd.md` (full), `gavi411-brain.md`
(full, all 69 decisions), `CLAUDE.md`, `HANDOFF.md` (full), `gavi411-gap-analysis.md`,
`gavi411-gap-analysis-followup.md`, the **live** G411 Jira backlog (10 Epics, 71 Tasks,
pulled fresh via JQL — not the stale `gavi411-jira-tree.md` snapshot), and the actual
running code in `server/` and `client/src/`.

**Methodology note, since it materially changed some findings**: the docs I was told to
read (`CLAUDE.md`, `HANDOFF.md`, `gavi411-brain.md`) turned out to be stale relative to
live Jira and the actual git log — `git log` shows PR #28 (G411-76) already merged to
`main`, but `CLAUDE.md`/`HANDOFF.md`/`brain.md`'s "known critical gap" framing (all three
describe G411-76 as unstarted/Open) predates that merge. Live Jira confirms G411-76 is
**Reconciled**, and reading the actual merged code in `server/middleware/auth.js` and
`scripts/promote-admin.js` confirms real work landed. I verified every ticket status
referenced below against live Jira and, where it mattered, against the actual code —
not against the (sometimes stale) narrative docs.

---

## Journey 1 — Friend, cold start (no invite → signed in, completed profile, can submit)

| Step | Actor | Concrete mechanism | Owning ticket | Status | Gap? |
|---|---|---|---|---|---|
| 1. Friend without an invite visits the homepage, has no way in | Friend | None found — no public "request access" form exists anywhere in `client/src/` (grepped, confirmed) | **G411-68** ("Request access" homepage form + admin review queue) | Open | **Yes** — the PRD's non-invited path has zero UI. A stranger literally cannot reach any entry point today. |
| 2. Gavi reviews the request-access submission, approves/declines | Gavi | None found — no admin review queue UI exists | G411-68 (same ticket, admin side) | Open | **Yes** — same gap, admin half. |
| 3. Approved person receives a real invite link with escrow passphrase in the URL fragment | System/Gavi | None found — no invite-generation UI or escrow-CSV export exists in `client/src/` or `server/` | G411-41 (user management — invites), tied to G411-28 (E2E escrow) | Both Open | **Yes** — the entire invite-creation mechanism (the actual PRD-described flow: "Gavi creates an invite → admin panel generates escrow passphrase → CSV export") doesn't exist. Confirmed via grep: no invite/escrow code anywhere in `server/`. |
| 4. Friend clicks the invite link, Clerk sign-up gates on it | System | **Undecided which mechanism** — G411-71's own description explicitly says the two candidate designs (app-side token gating vs. Clerk allowlist) were never chosen | G411-71 | **Reconciled in Jira**, but reading its own description: it only confirms the gap exists (`sign_up.mode: "public"`, no restriction active) and lays out two options to decide "at pickup" — no evidence either was ever implemented | **Yes, and notable**: G411-71 is marked Reconciled, but its own ticket text is unfinished/undecided prose, and no invite-token validation code exists in `server/` (grepped, confirmed). This reads as a Jira-status/actual-implementation mismatch worth flagging on its own. |
| 5. Friend completes Clerk sign-up (Google or email/username) | Friend | Clerk hosted `<SignIn />`, gated by `useUser().isSignedIn` in `App.jsx` | G411-66 | Reconciled | No — confirmed working in code (`App.jsx` lines 25-92). |
| 6. First authenticated request creates a real, correctly-populated Prisma `User` row (name/email from Clerk, not blank) | System | `requireAuth` in `server/middleware/auth.js`, calls Clerk's Backend API (`clerkClient.users.getUser`) | G411-76 | Reconciled | No — confirmed live in code: real name/email population, primary-email resolution, race-safe creation (P2002 recovery). |
| 7. Friend is prompted to complete their profile (real phone number, replacing the `pending-<clerkId>` placeholder; profile picture) before doing anything else | Friend | **No `/complete-profile` route exists anywhere in `App.jsx`'s view switcher** (`'list' \| 'new' \| 'install-help' \| 'detail'` — no `'complete-profile'` state) | G411-69 | **Open** (confirmed via live JQL — this is the current, correct owner; G411-70 was closed/subsumed into it, not built) | **Yes, and severe**: every signed-in friend today has a permanent `phoneNumber: "pending-<clerkId>"` DB value and no code path ever prompts them to replace it. The "first-login gate, not a someday-editable settings screen" design decision (brain.md decision #56) is fully unbuilt. |
| 8. Friend submits their first request | Friend | `NewRequest.jsx` → `POST /api/requests` | G411-18/19/21/22/23/65/74 | Reconciled | No — confirmed working end-to-end in code, including credit deduction (transactional, race-safe). |

**Journey 1 verdict**: broken well before the end. A friend with no invite cannot get one through any built mechanism (steps 1-4); a friend who *does* have a Clerk account (e.g., created directly by Gavi for testing, as the one real test user was) can sign in and submit a request, but permanently carries a fake phone number since nothing ever prompts for a real one (step 7). Step 8 onward works.

---

## Journey 2 — Friend, existing user (sign in → submit → reply from Gavi → notification → reply back)

| Step | Actor | Concrete mechanism | Owning ticket | Status | Gap? |
|---|---|---|---|---|---|
| 1. Friend signs in | Friend | Clerk `<SignIn />` / `useUser` gate | G411-66 | Reconciled | No |
| 2. Friend sees their request list (open/closed toggle, most-recent-closed fallback) | Friend | `RequestList.jsx`, `GET /api/requests` | G411-67 | Reconciled | No — confirmed working in code. |
| 3. Friend submits a request | Friend | `NewRequest.jsx` → `POST /api/requests` | G411-18-23/65/74 | Reconciled | No |
| 4. Gavi is notified of the new request (Telegram) | System | **None found** — grepped `server/` and `client/src/` for "telegram"/"Telegram"/"TELEGRAM": zero matches. `server/package.json` has no Telegram/HTTP-bot dependency. `NewRequest.jsx`'s own confirmation copy says "Gavi's been notified" but nothing sends that notification. | G411-50 (Telegram bot setup), G411-51 (trigger matrix) | Both Open | **Yes** — the app's own UI copy asserts something happens that doesn't. This is a real user-facing false claim, not just a missing backend feature. |
| 5. Gavi reads the request in an admin view | Gavi | **No admin UI exists at all.** Grepped `client/src/` for any admin screen/component — none found; the only "admin" references are the `role === 'ADMIN'` server-side filter branches in `requests.js`/`RequestList.jsx` comments. | G411-37 (admin list screen), G411-38 (admin detail screen) | Both Open | **Yes, severe** — there is no way for Gavi to see or act on requests through the app UI today. The only access path is direct DB queries or re-using the friend-facing `RequestDetail`/`MessageThread` UI as an admin (which requires the `ADMIN` role to be set, which now IS possible via `scripts/promote-admin.js`, but there's still no dedicated cockpit). |
| 6. Gavi replies in-thread | Gavi | `POST /api/requests/:id/messages` (owner-or-admin) | G411-24 | Reconciled | Route works and is admin-capable (confirmed in code — `req.user.role !== 'ADMIN'` check), but there's no admin-side UI surface to trigger it from except reusing the friend-facing detail screen manually. |
| 7. Friend sees a "Gavi replied" notification (Web Push) | Friend | **None found** — grepped for "push"/"Push"/"VAPID"/"webpush": zero real implementation matches (only comment/label text). No `web-push` dependency in `server/package.json`. No service-worker push-event handler beyond the bare install/PWA `sw.js` baseline. | G411-29 (Web Push delivery), G411-49 (subscribe flow), G411-51 (trigger matrix) | All Open | **Yes** — a friend has no way to know Gavi replied except manually reopening the app and checking. |
| 8. Friend replies back | Friend | `RequestDetail.jsx`'s compose control → `POST /api/requests/:id/messages` | G411-24/25/26 | Reconciled | No — confirmed working in code (text + image, sender-aware bubble alignment). |

**Journey 2 verdict**: friend-side round-trip (steps 1-3, 8) works. The entire admin-facing half (steps 4-7 — notification, cockpit, and friend notification-back) is unbuilt. A real back-and-forth conversation is only possible today by Gavi manually re-using friend-facing screens with an admin-promoted account and polling for replies with no push signal either direction.

---

## Journey 3 — Gavi, inviting someone specifically (not via request-access queue)

| Step | Actor | Concrete mechanism | Owning ticket | Status | Gap? |
|---|---|---|---|---|---|
| 1. Gavi decides to invite a specific person | Gavi | No UI — this is a real-world/out-of-app decision, correctly not code | n/a | n/a | No |
| 2. Gavi generates an invite link (with escrow passphrase in the URL fragment, per brain.md's E2E design) | Gavi | **None found** — no invite-creation route/screen exists anywhere in `server/` or `client/src/` (grepped) | G411-41 (user management, "invites" listed in scope) | Open | **Yes** — the entire invite-generation mechanism described in PRD §5/brain.md's escrow design has zero code. |
| 3. Gavi exports the escrow passphrase to a CSV for 1Password import | Gavi | **None found** | G411-41 or G411-28 (unclear which owns it — neither ticket explicitly claims the CSV export step) | Both Open | **Yes** — same gap as step 2, plus a real ownership ambiguity: G411-41's description doesn't mention escrow/CSV, G411-28's is about E2E crypto core, not the admin-side export UX. Worth Gavi's call on which ticket actually owns this. |
| 4. Friend clicks the link, Clerk sign-up gates on the invite being valid | System | Same as Journey 1 step 4 — **mechanism undecided** per G411-71's own description | G411-71 | Reconciled (but see Journey 1 note — text is unfinished/undecided) | **Yes** — same finding as J1. |
| 5. Invited person ends up signed in | Friend | Clerk `<SignIn />` | G411-66 | Reconciled | No, once they reach it. |

**Journey 3 verdict**: the entire journey the ticket asks to walk (Gavi-initiated, targeted invite) has **no built mechanism from step 2 onward** except the generic Clerk sign-in gate at the very end. Everything about "how does a specific person get a link" is unbuilt.

---

## Journey 4 — Gavi, request-access queue (stranger asks via homepage form → Gavi reviews → approve/decline → signed in)

| Step | Actor | Concrete mechanism | Owning ticket | Status | Gap? |
|---|---|---|---|---|---|
| 1. Stranger fills out a homepage "request access" form | Stranger | **None found** — no such form/route in `client/src/` | G411-68 | Open | **Yes** |
| 2. Gavi sees the submitted requests somewhere (queue view) | Gavi | **None found** | G411-68 | Open | **Yes** |
| 3. Gavi approves or declines | Gavi | **None found** | G411-68 | Open | **Yes** |
| 4. On approval, an invite is generated and sent | System/Gavi | Depends on Journey 3's step 2 mechanism, which also doesn't exist | G411-68 (says explicitly: "the actual Clerk invite-sending mechanism itself... is not covered by this task" — assumes it exists elsewhere) | Open, and its assumed dependency (G411-41's invite generation) is *also* unbuilt | **Yes, compounding** — even once G411-68 is built, it explicitly punts invite-sending to a mechanism (G411-41) that itself doesn't exist yet. Two gaps stacked, correctly scoped as separate tickets, neither built. |
| 5. Approved person ends up signed in | Stranger→Friend | Same Clerk flow as J1/J3 | G411-66 (works) + G411-71 (undecided gating) | Mixed | **Yes** — inherits J1/J3's gap. |

**Journey 4 verdict**: entirely unbuilt, correctly tracked as Open (G411-68), consistent with its ticket status. No surprises here — this is the one journey where Jira status and reality agree cleanly ("doesn't work yet" is accurately reflected as Open, not silently assumed done).

---

## Journey 5 — Gavi, day-to-day triage (admin view → sorted/filtered list → open one → reply → change status → close)

| Step | Actor | Concrete mechanism | Owning ticket | Status | Gap? |
|---|---|---|---|---|---|
| 1. Gavi opens the admin view | Gavi | **None found** — no distinct admin route/screen exists in `client/src/` at all | G411-37 (admin list screen) | Open | **Yes** |
| 2. Sees open requests sorted (urgency oldest-first default) / filtered / grouped by person | Gavi | **None found** — `RequestList.jsx` is the friend-facing screen, has no sort/filter/group controls, and (per G411-67's server code) an admin's `GET /api/requests` returns *every* request unsorted-by-urgency (only `orderBy: createdAt desc`) | G411-37 | Open | **Yes** — even the underlying data shape (all-requests-for-admin) exists server-side, but no cockpit UI consumes it the way the PRD describes. |
| 3. Opens one request | Gavi | Would have to reuse `RequestDetail.jsx` (friend-facing component), which lacks admin-only elements (private notes tab, status control) | G411-38 (admin detail screen) | Open | **Yes** |
| 4. Replies | Gavi | `POST /api/requests/:id/messages` works and is admin-capable server-side | G411-24 | Reconciled (route) / G411-38 (UI) Open | Partial — the API supports it, but there's no admin-specific UI affordance (e.g., distinguishing "replying as Gavi" from "replying as a friend" beyond the generic `MessageThread` bubble alignment, which does work per-viewer). |
| 5. Changes its status | Gavi | `PATCH /api/requests/:id` exists as a route shell (validates enum, writes it) but **no UI calls it anywhere** — grepped `client/src/`, no `fetch` to `PATCH /api/requests` found | G411-39 (status management controls), G411-30 (state machine) | Both Open | **Yes** — the backend primitive exists (G411-67's shell) but zero lifecycle rules (cancel/refund, close-confirm, reopen, auto-close, urgent-downgrade) and zero UI trigger it. |
| 6. Closes it (asks "did this resolve it?" → friend confirms) | Gavi/Friend | **None found** | G411-33 (close flow) | Open | **Yes** |
| 7. Private notes per request | Gavi | `Note` model exists in schema (correctly scoped, confirmed no visibility-flag gap), but **no route or UI reads/writes it** — grepped, no `/notes` route in `server/routes/`, `Note` never referenced in any `.jsx` file | G411-40 | Open | **Yes** |

**Journey 5 verdict**: the entire admin cockpit — the actual day-to-day operating surface for the whole product — does not exist. Every step blocked. This is accurately reflected as Open across G411-37/38/39/40/30/33, so it's an honest "not built yet" rather than a silent gap, but it's worth being explicit that essentially **none of Parent 5 (Admin Cockpit) or Parent 4 (Lifecycle) is usable today** beyond the raw `PATCH` shell and the message-send route.

---

## Journey 6 — Gavi, promoting a second admin

| Step | Actor | Concrete mechanism | Owning ticket | Status | Gap? |
|---|---|---|---|---|---|
| 1. Identify which user should become admin | Gavi | No UI to browse/search users at all (no user-management screen exists) | G411-41 (user management) | Open | **Yes** — Gavi would have to query the live DB directly (by email, since that now exists per G411-76) to find the right `clerkId`. |
| 2. Grant admin | Gavi | `node scripts/promote-admin.js <clerkId>` — a real, working, reproducible CLI script (confirmed by reading it) | G411-76 | **Reconciled** | **No, for the mechanism itself** — this genuinely works and is a deliberate, documented design choice (G411-76's own description: "no admin UI/route exists yet — that's a bigger, separate concern"). **Partial gap worth flagging anyway**: it's a CLI script requiring server-side shell access, not anything Gavi can do from a UI, and finding the right `clerkId` (step 1) has no supporting tool — so "promoting a second admin" today means Gavi manually cross-referencing Clerk's dashboard against a raw Neon query, then SSHing/running a local script. Functional, but a real, deliberately-scoped-out UX gap (correctly out of G411-76's stated scope, not a hidden one). |

**Journey 6 verdict**: works end-to-end via a deliberately narrow CLI escape hatch, honestly scoped that way in the ticket itself. Not a silent gap — flagged as its own known limitation in G411-76's description.

---

## Journey 7 — Friend, credit lifecycle (balance → decreases on submit → resets → hits zero → overdraft)

| Step | Actor | Concrete mechanism | Owning ticket | Status | Gap? |
|---|---|---|---|---|---|
| 1. Friend has some credit balance | Friend | `User.creditBalance Int` column exists in schema, defaults to `0` on creation (`requireAuth`'s `prisma.user.create`) | G411-45 (schema + display) | Open | **Yes, and notable**: new users are created with `creditBalance: 0`, not a real starting allotment per their group tag (Limited 2 / Regular 5 / Close 7) — there's no code anywhere that grants an initial balance. A brand-new friend literally cannot submit their first request (the `POST /` transaction throws 402 `Insufficient credit balance` below `1`) until some manual/future mechanism tops them up. This is a sharper version of "credits aren't built yet" — it's not neutral-absent, it's an active block on Journey 1/2's request-submission step for any genuinely new user. |
| 2. Friend sees their credit balance displayed | Friend | **None found** — grepped `client/src/` for "creditBalance"/"credit balance": zero UI matches | G411-45 | Open | **Yes** |
| 3. Balance decreases on request submission | System | `POST /api/requests` does a real, transactional, race-safe decrement (confirmed in code) | G411-23 | Reconciled | **No** — this specific mechanic genuinely works, assuming the friend already has balance (see step 1's gap). |
| 4. Balance resets on a monthly schedule, tiered by group tag | System | **None found** — no scheduler, no cron, no reset job/route anywhere. `server/package.json` has no `node-cron` or similar dependency. | G411-46 | Open | **Yes** — and per the gap analysis's F1 finding (still accurate), no shared scheduling mechanism has been decided for this vs. G411-35's auto-close job either. |
| 5. Friend hits zero credits | Friend | The 402 error path exists and is real (confirmed in `requests.js`), but the frontend's handling of it is generic (`submitError` text), not a dedicated "you're out of credits" UX | G411-47 (overdraft) | Open | Partial — the block itself works, but there's no differentiated messaging or overdraft offer surfaced to the friend when it fires. |
| 6. Friend sees the "request anyway" overdraft option | Friend | **None found** — no UI or backend logic implements the one-per-period overdraft; the design (top balance to 1, consume immediately) is captured only as a Jira comment on G411-47 per the gap-analysis's F3 finding, folded into the Description since (confirmed by reading G411-47... not fetched directly this pass, but gap-analysis-followup confirms it was folded in) | G411-47 | Open | **Yes** |

**Journey 7 verdict**: nothing in the credit lifecycle works as a self-sustaining loop yet — deduction-on-submit is the one real piece, but with no initial grant, no reset, and no overdraft, a new friend's credit balance is a dead-end at `0` from the moment their account is created. This is arguably a **sharper, more urgent gap than the docs currently frame it as** — `gavi411-brain.md`/`HANDOFF.md` discuss credits as "Parent 6, not started yet" in the abstract, but don't call out that the *current* default (`creditBalance: 0` on creation, no grant anywhere) actively blocks every new user's very first request today, not just "credits features are missing."

---

## Journey 8 — Messaging security (fallback or target design, whichever is being built)

| Step | Actor | Concrete mechanism | Owning ticket | Status | Gap? |
|---|---|---|---|---|---|
| 1. A message is sent | Friend/Gavi | `POST /api/requests/:id/messages`, stores `content` as **plain text**, `imageUrl` as a **plain Cloudinary URL** — confirmed by reading `prisma/schema.prisma`'s `Message` model (`content String`, `imageUrl String?`, no encryption applied anywhere in `requests.js`) | G411-27 (encryption-at-rest, the "guaranteed v1 fallback") | **Open** | **Yes** — neither the fallback (EaR) nor the target (E2E) design is implemented. Messages are stored and readable in plaintext in the live DB today. |
| 2. Message is stored | System | Plain Postgres row, no `crypto` encrypt/decrypt anywhere in the message-write path (confirmed) | G411-27 | Open | **Yes**, same gap. |
| 3. Message is later read back | Friend/Gavi | `GET /api/requests/:id` returns the plaintext `content`/`imageUrl` directly | n/a (works, but only because nothing is encrypted to begin with) | n/a | N/A — read-back "works" only in the sense that there's nothing to decrypt. |
| 4. (If E2E) friend loses their device, needs recovery | Friend | **None found** — no keypair generation, no IndexedDB private-key storage, no device-linking/approval flow, no escrow flow anywhere in `client/src/` or `server/` | G411-28 (target E2E) | Open | **Yes** — entire feature unbuilt, correctly tracked as Open and explicitly deferred ("attempted after core app works, not commit-triggered" per its own description). Not a silent gap — accurately reflected. |

**Journey 8 verdict**: **currently building neither the fallback nor the target design** — this is worth flagging distinctly from "G411-27/28 are Open, that's expected." The PRD calls encryption-at-rest the "guaranteed v1" — i.e., the one messaging-security tier that's supposed to be non-negotiable — and it is not built, while G411-24/25/26 (the message *feature* itself) are fully Reconciled and shipping real plaintext messages (including images) to production today. This is a real product-security gap in what's currently live, not just an unstarted future ticket.

---

## Journey 9 — Other PRD §6 features not covered above

Checked PRD §6.1/§6.2/§6.3 tables against the 8 journeys above and the Jira backlog for anything not yet walked.

| PRD feature (§6) | Owning ticket(s) | Status | Gap? |
|---|---|---|---|
| §6.1 PWA installability (manifest + SW + "install on iPhone" help page) | G411-15 (infra) + install-ios wiring folded into G411-67 | Both Reconciled | No — confirmed: `InstallHelp.jsx` exists and is wired into `App.jsx`'s view switcher, service worker registers in `main.jsx`. |
| §6.2 Presence control (manual online/offline toggle) | G411-43 | Open | **Yes** — no toggle exists anywhere (grepped, zero "presence" matches in code). Also, per the already-filed F5 finding (gap-analysis, resolved as "fold into G411-67's future scope"): there is still no friend-facing surface showing presence status even once G411-43 builds the toggle — G411-67 is Reconciled now and its shipped code has no presence indicator, so F5's resolution ("fold into that screen's scope") did not actually happen. **This re-opens F5** — it was marked resolved in the followup tracker on the assumption G411-67 would pick it up, but G411-67 is now done and didn't. |
| §6.2 Trigger/keyword admin UI (live-editable, no redeploy) | G411-42 | Open | Consistent — Trigger table is seeded via `prisma/seed.js` only, no live-edit UI, correctly tracked Open. |
| §6.2 Gavi-initiated requests (paste content, generate share link) | G411-44 | Open | Consistent, correctly tracked. |
| §6.3 V2/Stretch (auto-Shabbat, reminders, guest view, post-close reaction, tips link) | G411-58 through 62 | All Open | Consistent — explicitly deferred per PRD, no gap. |
| G411-77 (Full UI/UX pass), G411-78 (aria-live region), G411-79 (video/doc attachments), G411-80 (profile screen + Clerk push-back sync) | — | All Open | Consistent, correctly tracked, not silently assumed done. |

---

## Flat list of every gap found, with severity

**Blocks a whole journey:**
1. No "request access" homepage form or admin review queue exists (J1 step 1-2, J4 entirely) — G411-68, Open.
2. No invite-generation mechanism (link + escrow passphrase + CSV export) exists anywhere in code (J1 step 3, J3 steps 2-3) — G411-41/G411-28, Open, and the CSV-export step has no clear single owner between them.
3. Invite-gating mechanism for Clerk sign-up is undecided/unimplemented despite G411-71 showing **Reconciled** in Jira — the ticket's own description only lays out two candidate designs, neither committed to code (J1/J3/J4 step 4). **This is a Jira-status-vs-reality mismatch worth flagging on its own**, separate from the underlying feature gap.
4. No `/complete-profile` flow exists despite the correct owning ticket (G411-69) being Open — every signed-in friend today permanently carries a fake `phoneNumber: "pending-<clerkId>"` (J1 step 7).
5. No admin cockpit exists at all — no admin list screen, no admin detail screen, no status controls, no private notes UI (J2 step 5, J5 entirely) — G411-37/38/39/40, all Open.
6. No lifecycle logic exists beyond the raw `PATCH` shell — no close flow, no cancel/refund, no reopen rules, no auto-close, no urgent-downgrade (J5 steps 5-6) — G411-30/31/32/33/34/35/36, all Open.
7. No Telegram notification to Gavi exists despite the app's own UI copy ("Gavi's been notified") asserting it happens (J2 step 4) — G411-50/51, Open. This is a **live false claim in shipped UI text**, not just an absent feature.
8. No Web Push notification exists in either direction (J2 steps 4/7) — G411-29/49/51, Open.
9. **New users are created with `creditBalance: 0` and nothing anywhere grants an initial tiered allotment** — a genuinely new friend cannot submit even their first request until manually topped up (J7 step 1). This is more acute than "credits feature not built" — it's an active block on the core intake path for any real new user today.
10. No credit reset scheduler, no overdraft mechanism (J7 steps 4-6) — G411-46/47, Open.
11. Neither the encryption-at-rest fallback nor the target E2E design is implemented — all messages/images are stored and transmitted in plaintext today, despite the "guaranteed v1" framing for encryption-at-rest (J8 steps 1-2) — G411-27, Open, while the messaging feature itself (G411-24/25/26) is fully live in production.

**Partial workaround exists:**
12. Admin can technically read/reply to requests by reusing the friend-facing `RequestDetail`/`MessageThread` screens once promoted to `ADMIN` via `scripts/promote-admin.js` — functional but not a cockpit, no sort/filter/group, no notes, no status controls (J2 step 5-6, J5 steps 3-4).
13. Promoting a second admin works via a real, reproducible CLI script (`scripts/promote-admin.js`), but finding the right `clerkId` to promote has no supporting UI — Gavi must cross-reference Clerk's dashboard against a raw DB query manually (J6). Correctly and honestly scoped out of G411-76 in its own description, not a silent gap.
14. Hitting zero credits does correctly 402 server-side, but the friend sees only a generic error message, not differentiated "out of credits" UX or an overdraft offer (J7 step 5).
15. **Presence indicator gap re-opened**: gap-analysis finding F5 was marked "resolved" on the assumption the future G411-67 would surface a presence indicator on the home screen; G411-67 is now Reconciled and its shipped `RequestList.jsx` has no such indicator. The toggle itself (G411-43) is still correctly Open, but F5's "no action needed, folded into G411-67" resolution needs re-checking now that G411-67 has actually shipped without it.

**Cosmetic / low severity:**
16. `RequestList.jsx`'s `CLOSED_STATUSES` grouping (`CLOSED`/`CANCELLED`/`SELF_SOLVED`) is a frontend-hardcoded copy of backend lifecycle knowledge, self-flagged as provisional in its own code comment — will need to stay in sync if G411-30's real state machine changes shape. Not user-facing today.
17. `Request.updatedAt` has no `@updatedAt` directive in `schema.prisma` (confirmed by reading it), so `PATCH` never actually bumps it — a latent bug for any future status-sorting UI (G411-37/39), not yet exercised since nothing calls `PATCH` from the UI today.

---

## Notable process finding, not a product gap but worth flagging back

G411-71 (invite-gating mechanism) is marked **Reconciled** in live Jira, but reading its own Description shows it only documents that the gap exists and lays out two undecided implementation options — no code implementing either exists anywhere in `server/` or `client/src/` (confirmed by grep and by reading `main.jsx`/`App.jsx`/`server/server.js`/`server/routes/requests.js` in full — no invite-token validation, no Clerk allowlist API calls). This looks like either (a) a ticket that was closed prematurely without the actual mechanism being built, or (b) real code that exists somewhere I didn't find. Given the concrete grep/read evidence, (a) seems more likely, but this is exactly the kind of status-vs-reality mismatch the task brief asked to catch and flag rather than resolve — recommend Gavi (or a session that picks this back up) verify directly rather than trust the Reconciled status.
